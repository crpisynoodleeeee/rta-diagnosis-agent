# 智能投放数据交互 · 补丁 v0.1：执行边界分级与人工确认点

> 补丁对象：《智能投放数据交互.txt》（7 场景 Agent 蓝图，位于 `D:\AI_Codex\Projects\program\`）
> 目的：补全原蓝图缺失的「执行边界」——哪些动作能自动、哪些必须人工确认、哪些自动后需可回滚。
> 核心矛盾：原蓝图中"自动执行"的标准不一致（场景三"自动调价/暂停"、场景四"人工选级别"、场景六"采纳后执行"），缺少统一分级规则。

---

## 一、总原则：增支 ≠ 减支

广告投放的执行自动化，核心判断标准只有一条：

> **"增加支出"和"减少支出"的自动化边界必须不对称。**

- **增支**（提价、放量、加预算、换素材抢更多流量）→ 错了会烧钱，损失无上限 → **必须人工确认**
- **减支/止损**（降价、暂停、减预算、阻断）→ 错了只是少花/停花，损失有上限 → 可自动 + 护栏

这一条主线，统一解释原蓝图中所有"看起来矛盾"的执行模式。

---

## 二、四级执行分级框架

| 级别 | 定义 | 典型动作 | 触发方式 |
|---|---|---|---|
| **L0 只读/只告警** | 零资金风险，只通知不落地 | 企微告警、报表推送、趋势报告 | 自动 |
| **L1 人工确认后执行** | 有资金风险，人点确认才落地 | 提价、加预算、素材替换、放量、采纳预测方案 | 人工 gate |
| **L2 有护栏的自动** | 低风险可逆，带护栏自动执行，事后可查可撤销 | 降价、减预算、调价监测任务创建 | 自动 + 护栏 |
| **L3 紧急止损全自动** | 不执行损失更大，秒级自动 + 强留痕 | 合规暂停、风控阻断 | 自动（唯一例外） |

**关键**：L3 是唯一"无人工确认直接动钱"的场景，且只做止损、不做增益。除此之外，所有"动钱"动作都要经过 L1 人工 gate 或 L2 护栏。

---

## 三、L2 护栏的四要素（不可省略）

"L2 有护栏自动"必须明确四样，否则会滑回"无护栏自动"：

1. **幅度上限**：单次降价 ≤ 20%，避免误伤
2. **方向限制**：L2 只允许减支方向，不允许增支方向
3. **白名单**：只对指定计划/账户自动，新计划默认走 L1
4. **可撤销**：所有 L2 自动动作必须留痕，运营可一键回滚

---

## 四、逐动作定级表（覆盖原蓝图全部执行环节）

| 环节 | 动作类型 | 方向 | 分级 | 理由 |
|---|---|---|---|---|
| 告警推送（一⑫、三⑦a） | 通知 | — | **L0** | 纯通知，零资金风险 |
| 创建监测/管控任务（一⑩、二⑫、三③、六⑪） | 元操作 | — | **L2** | 只建规则不动钱，但规则会触发后续执行，需可查可撤销 |
| 出价调整（提价）（一⑪、三⑦b、六⑫） | 调价 | 增支 | **L1** | 提价烧钱，必须人工确认 |
| 出价调整（降价）（同上） | 调价 | 减支 | **L2** | 降价少花钱，可自动，但需降价下限 + 单次幅度上限护栏 |
| 暂停投放（三⑦b、四⑪） | 暂停 | 减支/止损 | **L3（紧急）/ L2（常规）** | 合规风险秒级自动；常规省钱暂停可 L2 |
| 阻断流量（四⑫） | 阻断 | 止损 | **L3** | 风控/监管触发，不执行损失更大，必须立即 |
| 素材替换（六⑫） | 换素材 | 增支 | **L1** | 换素材带来流量变化，需人工确认 |
| 预算重分配（加）（六⑫） | 调预算 | 增支 | **L1** | 加预算烧钱，必须人工 |
| 预算重分配（减）（六⑫） | 调预算 | 减支 | **L2** | 减预算可自动 |

---

## 五、人工确认点清单

### 5.1 三类确认点（统一口径）

| 类型 | 含义 | 时机 |
|---|---|---|
| **A · 事前确认** | 执行前必须人点"采纳/批准"才落地 | L1 增支动作的 gate |
| **B · 事后干预** | 系统自动执行后，人可一键回滚/撤销 | L2 自动动作的保险 |
| **C · 配置授权** | 一次性/周期性维护白名单、护栏、规则 | 建规则时 |

一句话：**A 卡在"钱花出去之前"，B 兜在"钱花错之后"，C 管在"允许谁自动"。**

### 5.2 逐场景确认点

#### 场景一 · 实时竞价感知
| # | 确认点 | 类型 | 动作 | 分级 |
|---|---|---|---|---|
| 1-1 | 提价建议的「采纳」按钮 | A | 出价上调 | L1 |
| 1-2 | 降价动作的回滚入口 | B | 出价下调（已自动） | L2 兜底 |
| 1-3 | 竞价调价白名单维护 | C | 哪些计划允许 L2 自动降价 | 配置 |

#### 场景二 · 效果预测
| # | 确认点 | 类型 | 动作 | 分级 |
|---|---|---|---|---|
| 2-1 | 预测报告「采纳方案」 | A | 采纳后创建预算管控任务 | L1 |
| 2-2 | 预算管控任务的护栏配置 | C | 管控任务触发执行时的幅度/方向限制 | 配置 |

> 场景二本身偏分析，执行风险后移到"采纳方案"这个点——**采纳 = 确认"按预测执行"**，必须显式，不能默认采纳。

#### 场景三 · 智能预警（重点）
| # | 确认点 | 类型 | 动作 | 分级 |
|---|---|---|---|---|
| 3-1 | 预警规则创建时的「执行方式」选择 | C | 选"仅告警 / 自动暂停 / 调价需确认" | 配置 |
| 3-2 | **调价建议（提价）的「确认」** | A | 提价 | L1 |
| 3-3 | 自动暂停的「恢复」入口 | B | 暂停后恢复投放 | L2 兜底 |
| 3-4 | 告警后的「确认/调整/忽略/退回」 | A | 运营处理告警 | L0→L1 交接 |

> 场景三是原蓝图问题最大的地方（⑦b "自动执行调价/暂停"笼统）。拆开后核心是 **3-2：提价必须有人点，暂停可以自动**。

#### 场景四 · 应急暂停
| # | 确认点 | 类型 | 动作 | 分级 |
|---|---|---|---|---|
| 4-1 | 「选择操作级别」（全量/渠道/账户/封锁预算） | A | 决定暂停范围 | 已有（人工） |
| 4-2 | **恢复快照的「确认恢复」** | A | 暂停后恢复 | L3 的事后 gate |
| 4-3 | 合规暂停的授权范围 | C | 哪些风控/监管信号触发自动暂停 | 配置 |

> 场景四已经是"人工干预"，缺的不是"执行前确认"（已有），而是 **4-2 恢复时的确认**——自动暂停可以秒级，但恢复投放绝不能自动。

#### 场景五 · 趋势分析
| # | 确认点 | 类型 | 动作 | 分级 |
|---|---|---|---|---|
| 5-1 | 「异常趋势 → 创建追踪任务」可选确认 | C | 追踪任务是否自动创建 | 配置 |

> 场景五纯分析，几乎无确认点。唯一一处是"识别出异常趋势后要不要自动追踪"——建议默认**只出报告不自动追踪（L0）**，追踪由运营手动触发。

#### 场景六 · 优化建议
| # | 确认点 | 类型 | 动作 | 分级 |
|---|---|---|---|---|
| 6-1 | 「一键采纳」 | A | 采纳建议集 | L1 |
| 6-2 | **素材替换 / 预算加码 的单独确认** | A | 增支动作 | L1 |
| 6-3 | 出价下调 / 预算缩减 的回滚 | B | 减支动作（已自动） | L2 兜底 |

> 场景六的「一键采纳」有问题：把"素材替换（增支）+ 出价调整（分方向）+ 预算重分配（分增减）"打包成一个按钮。**建议拆成"采纳全部（仅减支类）"和"增支类需逐项确认"**，否则一键采纳等于绕过所有人工 gate。

#### 场景七 · 自然语言查询
| # | 确认点 | 类型 | 动作 | 分级 |
|---|---|---|---|---|
| — | 无 | — | 纯查询，零执行 | L0 |

> 场景七唯一接近"确认"的是口径修改，但那属于**数据治理**，不是投放执行，单列，不进本清单。

### 5.3 汇总总表

| 确认点 | 所属场景 | 类型 | 关键动作 | 分级 |
|---|---|---|---|---|
| 提价「采纳/确认」 | 一、三、六 | A | 出价上调 | L1 |
| 素材替换确认 | 六 | A | 换素材 | L1 |
| 预算加码确认 | 六 | A | 加预算 | L1 |
| 预测方案「采纳」 | 二 | A | 按预测执行 | L1 |
| 暂停范围选择 | 四 | A | 定暂停级别 | 已有 |
| 恢复快照「确认恢复」 | 四 | A | 恢复投放 | 事后 gate |
| 告警「确认/调整/忽略/退回」 | 三 | A | 处理告警 | L0→L1 |
| 降价/暂停 回滚入口 | 一、三、六 | B | 撤销自动动作 | L2 兜底 |
| 白名单/护栏/规则维护 | 一、二、三、四、五 | C | 授权自动范围 | 配置 |

---

## 六、核心结论

全蓝图真正"必须人工确认"的动作只有 4 类：**提价、换素材、加预算、采纳预测方案**。其余（降价、暂停、减预算）可以 L2 自动 + 事后回滚，唯一例外是风控合规暂停走 L3 秒级自动。

---

## 七、原蓝图需要修改的 3 处

1. **场景三 ⑦b「自动执行调价/暂停」** → 拆成两条：
   - `⑦b-1 自动暂停（L2/L3）`
   - `⑦b-2 调价建议（L1，人工确认后执行）`
2. **场景一 ⑪「执行出价调整」** → 默认只告警（L0）+ 生成调价建议（L1）；仅"降价"方向且幅度在护栏内，才允许 L2 自动。
3. **场景六 ⑫「执行素材替换/出价调整/预算重分配」** → 三个动作不该同等待遇，拆开：素材替换 L1、出价调整分方向、预算重分配分增减。

---

## 八、后续待补（本补丁之外）

1. **诊断归因层**：原蓝图缺"感知（出事）→ 诊断（为什么）→ 建议（怎么办）"中间的归因环节，是结构性空白，需另起一节。
2. **Agent 协同与冲突仲裁**：7 类 Agent 可能对同一计划同时操作，需定义优先级与互斥规则。
