# 项目三：贴图与页面结构参考清单

> 用途：作为新版 Demo 重构时的"母版依据"。核心结论是——**新版必须"在真实平台 RTA 页面里长出一个诊断抽屉"，而不是另起一套后台**。
> 依据：`R (2)/R/R1.txt`（RTA 基础能力）、`R (2)/R/R2.txt`（RTA 华为接入及功能优化 PRD）、`R (2)/R/PICTURE`（22 张真实截图）。
> 说明：本清单的页面结构来自 R1/R2 文档原文，是权威口径；截图文件名对应关系部分基于 WorkBuddy 记忆与文档推断，标注【待确认】处需人工逐图核对。

---

## 1. 真实平台的 RTA 页面地图（来自 R1/R2 文档）

真实平台里，RTA 相关功能挂在 `RTA` 菜单下，共 5 个页面：

| # | 页面 | 入口路径 | 页面类型 |
| --- | --- | --- | --- |
| P1 | 策略工厂 | RTA → 策略工厂 | 独立列表页 + 右侧抽屉（新建/编辑） |
| P2 | RTAID 配置 | RTA → 基础配置 → RTAID 配置 | 独立列表页 + 右侧抽屉（新增/编辑） |
| P3 | RTA 实验 | RTA → RTA 实验 | 列表页 + 右侧抽屉（步骤流） |
| P4 | RTA 报表 | RTA → 数据中心 → RTA 报表 | 独立报表页（核心指标卡 + 筛选 + 明细） |
| P5 | RTA 执行日志 | RTA → 数据中心 → RTA 执行日志 | 列表页 + 右侧详情抽屉 |

> 关键认知：真实平台的 RTA 域**没有"投放管理列表"这个页面**，也没有"预算/消耗/达成率"这种投放层字段——那是项目三为讲"有预算但跑不出去"这个故事，在投放侧抽象出来的作品集扩展层（对象模型文档 §4 已注明"Project 是作品集扩展对象"）。
>
> 因此"贴图"的正确姿势是：**列表页借 RTAID 配置列表的结构，抽屉借 RTA 执行日志详情抽屉的结构，诊断内容借 RTA 报表的指标体系**，把作品集的"预算/消耗/达成率"作为列表字段挂上去。

---

## 2. 三个母版页面 + 必须照抄的结构元素

### 母版 A：列表页 → 参考「RTAID 配置列表」（P2）

**列表字段（R1/R2 原文，新版 Demo 的表格列应以此为骨架）：**

| 真实 RTAID 配置列表字段 | 新版 Demo 的处理 |
| --- | --- |
| 媒体 | ✅ 保留 |
| RTAID | ✅ 保留 |
| BidURL | ✅ 保留（或折叠到详情） |
| 媒体账户 | ✅ 保留 |
| 状态 | ✅ 保留 |
| 配置 QPS | ✅ 保留 |
| 绑定账号数 | 可替换为「关联项目/单元」 |
| 关联实验数 | ✅ 保留 |
| 创建人 | 可选 |
| 更新时间 | ✅ 保留 |
| 操作 | ✅ 保留（新增「发起诊断」按钮） |

**页面结构元素（必须照抄）：**

1. 顶部「全局提示」：`QPS 配置上限：ICON + 当前系统 QPS 上限为 XXXX`（R1 原文）
2. 顶部「QPS 概览卡片」：3 张卡 —— 总配置 QPS / 总实验已占用 QPS / 总剩余 QPS（R2 §9.5，替换 v2 自造的"RTA 总数/异常 RTA/完成率偏低/本月发起诊断"）
3. 全局筛选：媒体（下拉多选，默认"全部媒体"）、RTAID 搜索框、状态筛选（上线/下线）
4. 列表操作按钮：新增 RTAID / 查看 / 编辑 / 上线 / 下线 / 「系统配置」（独立按钮）
5. **作品集扩展**：在列表上叠加"投放项目维度"——项目名称、日预算、实际消耗、预算达成率、异常状态，异常行红底 + 「发起诊断」按钮

> 关键取舍：v2 自造了"RTA 总数/异常 RTA/完成率偏低/本月发起诊断"4 张卡 + "RTA 投放诊断（NEW 徽标）"独立菜单项，这是"另起后台"的典型症状。新版应**去掉 NEW 徽标菜单项**，把诊断入口做成列表行级「发起诊断」按钮 + 顶部"发起诊断"入口，长在 RTAID 配置页内。

### 母版 B：右侧抽屉 → 参考「RTA 执行日志详情抽屉」（P5）+「新建/编辑 RTAID 抽屉」（P2）

**执行日志详情抽屉的分节结构（R1 原文，诊断抽屉的七段式应映射到此体系）：**

| RTA 执行日志详情分节 | 新版诊断抽屉对应 |
| --- | --- |
| 请求基础信息（RTA 请求 ID、请求时间、媒体、RTAID、媒体账户、设备 ID 类型） | → 抽屉顶部元信息 + §7 技术详情 |
| 实验匹配信息（实验名称/ID/类型/状态/实验组/分桶） | → §7 技术详情 |
| 策略执行链路（策略组、组内逻辑、策略名称、内部/媒体侧策略 ID、类型、参竞策略、执行结果、是否命中、是否短路、耗时） | → §5 原因树详情（策略与参竞节点） |
| 出价执行信息（出价方式、配置模式、价格类型、模型字段/分值/命中区间、最终出价） | → §5 原因树（媒体竞价节点）+ §7 技术详情 |
| 兜底执行信息（是否触发异常兜底/空设备 ID 兜底、兜底动作、原因） | → §5 原因树（实验与分桶节点） |
| 响应信息（最终决策、响应状态、响应耗时、失败原因） | → §4 证据时间线 + §7 技术详情 |

**抽屉结构元素（必须照抄）：**

1. 右侧滑入，宽度 720px（v2 已对）
2. 顶部：媒体/账户/项目/RTAID 面包屑 + 诊断标题 + 状态徽标 + 关闭按钮
3. 内容分节，逐层展开（管理摘要默认展示 → 运营解释展开 → 技术证据折叠）
4. 底部固定操作区：修改方案 / 拒绝 / 批准方案

### 母版 C：诊断内容 → 参考「RTA 报表」（P4）指标体系

**核心指标卡（资源口径，R1 原文）：**

当前 QPS / 峰值 QPS / QPS 使用率 / 平均响应耗时 / P95/P99 / 成功率 / 失败率 / 超时率

> 这些是真实平台的告警指标，也是诊断规则表 D3/D10 的阈值来源。新版诊断抽屉的"影响概览"和"原因树指标"应**复用这套口径**，而不是自造"请求量/参竞率/CPA 对比"。

**分析口径指标（使用情况）：**

请求总量 / 执行成功数 / 兜底执行数 / 参竞数·参竞率 / 拒绝数·拒绝率 / 策略命中数·命中率 / 平均出价系数·CPA·CPC

**明细数据维度：**

RTAID / 媒体 / 关联媒体账户 / 策略名称·ID / 媒体侧策略 ID / 策略类型 / 参竞策略 / 实验名称·ID / 实验类型 / 实验组 / 分桶 / 出价方式 / 出价配置模式 / 异常兜底

---

## 3. 截图文件对应关系（部分【待确认】）

PICTURE 目录共 22 张，已确认 2 张：

| 截图文件 | 对应页面 | 依据 | 状态 |
| --- | --- | --- | --- |
| `企业微信截图_17866879325577.png`（178KB） | RTAID 配置列表页（顶部 GROWTHPLATFORM 横排 + AI创意 选中态 + 蓝色按钮） | WorkBuddy 记忆 | ✅ 已确认 |
| `企业微信截图_17866865728109.png`（252KB） | 右侧抽屉（720px 滑入） | WorkBuddy 记忆 | ✅ 已确认 |

其余 20 张【待确认】，建议按大小粗分后人工核对：

| 大小段 | 数量 | 推测内容 | 建议核验 |
| --- | --- | --- | --- |
| >150KB | 约 13 张 | 完整页面（列表/报表/抽屉全貌） | 逐一打开确认是 P1-P5 哪个页面 |
| 30-75KB | 约 6 张 | 局部区块（字段/筛选/卡片/提示） | 确认属于哪个页面的哪个区块 |
| <20KB | 1 张 | 小图（图标/字段/局部） | 确认用途 |

> ⚠️ 本环境当前模型无法读图，无法逐张标注。**这 20 张的精确对应需要你（或一个能读图的会话）逐张打开确认**，然后把文件名 + 页面 + 要照抄的元素补进下面的表，作为给 WorkBuddy 提示词的最终贴图锚点。

---

## 4. 给 WorkBuddy 的"贴图"指令要点（可写进提示词）

1. **列表页母版** = RTAID 配置列表页（P2）：照抄顶部 QPS 概览卡（3 张）、全局提示、筛选区、列表字段骨架、操作按钮区；在其上叠加"投放项目 + 预算/消耗/达成率 + 发起诊断"。
2. **抽屉母版** = RTA 执行日志详情抽屉（P5）+ 新建 RTAID 抽屉（P2）：右侧 720px 滑入、顶部分节元信息、七段式映射到"请求基础信息/实验匹配/策略执行链路/出价执行/兜底执行/响应信息"、底部固定操作区。
3. **指标口径** = RTA 报表（P4）核心指标卡（资源口径）+ 使用情况指标（分析口径），不做自造指标。
4. **去营销化**：去掉"RTA 投放诊断（NEW 徽标）"独立菜单项、去掉自造指标卡，诊断入口做成列表行级按钮。
5. **明确禁止**：不新增独立一级/二级菜单；不造与截图无关的导航、卡片、字段名。

---

## 5. 与既有文档的联动

- 本清单的字段骨架 → 已体现在《RTA 对象模型与字段清单 v0.1》O3（RTAID）、O5（策略）、O7/O8（实验/分桶）、O10（执行日志）、O11（报表指标）。
- 诊断抽屉七段式 → 已对齐《Agent 输入输出结构契约 v0.2》§3 输出结构。
- 诊断规则阈值 → 已对齐《MVP 诊断规则表 v0.1》§2。
- 新版 Demo 重构时，三份文档 + 本清单共同作为输入，避免再次"字段对不上"。
