# RTA 对象模型与字段清单 v0.1

> 数据来源：`R (2)/R/R1.txt`（RTA 基础能力）、`R (2)/R/R2.txt`（RTA 华为接入及现有功能优化 PRD）、`R (2)/R/R_connect.txt`（小米/VIVO/OPPO/荣耀 RTA 能力适配）。
> 用途：作为 MVP 诊断规则表、Agent 输入输出结构、PRD 和数据建模的共同基础。
> 口径约定：保留原始字段名称；不确定的媒体侧字段标注【待核验】；本模型为公开 Demo 的 Mock 建模，不等同于任何真实平台实现。

---

## 1. 对象总览与实体关系

```text
媒体账户 (MediaAccount) ──绑定──> RTAID ──绑定──> 媒体账户
                                    │
                                    ├──引用──> 策略 (Strategy)          [策略绑定 RTAID]
                                    ├──引用──> 实验 (Experiment)        [实验绑定 RTAID]
                                    └──产生──> 执行日志 (ExecutionLog)  [按 RTAID 归属]

实验 (Experiment)
  ├── 1 个对照组 + N 个实验组 (ExperimentGroup)
  ├── 每组分配 1..N 个分桶 (Bucket)，共 10 桶
  └── 每组配置 1..N 个策略组 (StrategyGroup) ──> 1..N 条策略 (Strategy)

报表 (Report) = 按 维度（RTAID/媒体/账户/策略/实验/实验组/分桶/出价）× 指标 聚合
```

对象清单：

| # | 对象 | 来源模块 | 核心作用 |
| --- | --- | --- | --- |
| O1 | 媒体 Media | R1/R2/R_connect | 能力边界（出价、分桶、RTAID 映射） |
| O2 | 媒体账户 MediaAccount | R1/R2 | RTAID 绑定主体 |
| O3 | RTAID | R1/R2/R_connect | 策略、实验、请求归属的基础资源 |
| O4 | RTAID 系统配置 | R2 | 平台分桶 ID、系统 QPS 上限 |
| O5 | 策略 Strategy | R1/R2 | 可复用的参竞/出价规则资产 |
| O6 | 数据源与字段 DataSource/Field | R1/R2 | 用户字段/模型字段配置 |
| O7 | 实验 Experiment | R1/R2 | 普通实验 / AB 实验 |
| O8 | 实验组与分桶 Group/Bucket | R1/R2 | 对照组/实验组 + 10 桶分配 |
| O9 | 策略组 StrategyGroup | R1/R2 | 实验内策略组合容器 |
| O10 | 执行日志 ExecutionLog | R1 | 单次请求全链路证据 |
| O11 | 报表指标 Metrics | R1 | 资源口径 + 分析口径指标 |

---

## 2. 对象字段清单

### O1 媒体 Media

| 字段 | 说明 | 来源 |
| --- | --- | --- |
| 媒体名称 | 巨量、腾讯、华为、小米、VIVO、OPPO、荣耀 | R2/R_connect |
| RTAID 映射字段 | 巨量/腾讯：RTAID；华为：RTAID；小米：token；VIVO：type/advertiser_account（平台预设）；OPPO：ad_type_list/agree_type；荣耀：rtaId | R_connect §3.2 |
| 支持出价方式 | 小米/OPPO：仅出价系数；VIVO/荣耀：出价系数+CPC+CPA/oCPX；华为：出价系数+CPA+CPC；巨量/腾讯：沿用现有 | R_connect §14 |
| 支持分桶方式 | 巨量/腾讯：平台分桶+客户分桶；华为/小米/VIVO/OPPO/荣耀：仅客户分桶 | R_connect §9.4 |
| 签名校验 | 华为需 SecretKey（HmacSha256(requestId+requestTime+oaid)）；OPPO/VIVO 验签默认不开通 | R2 §9.4 / R_connect |

### O2 媒体账户 MediaAccount

| 字段 | 说明 | 来源 |
| --- | --- | --- |
| 媒体账户 ID | 平台侧唯一标识 | R1 |
| 媒体侧账户 ID | 媒体侧 ADVERTISER_ID【待核验】 | mock 标注 |
| 账户名称 | 展示名 | R1 |
| 所属代理商 | 代理商筛选维度 | R2 |
| 绑定对象 | 默认媒体账户；小米场景绑定创意（token 与创意绑定） | R_connect §4.2 |

绑定规则：一个 RTAID 可绑定多个账户；上线状态的 RTAID 必须绑定至少一个媒体账户；编辑时不允许删除全部账户（至少保留一个）。

### O3 RTAID

| 字段 | 类型 | 必填 | 说明 | 来源 |
| --- | --- | --- | --- | --- |
| 媒体 | 下拉单选 | 是 | 决定字段联动 | R2 §9.2 |
| RTAID | 输入框 | 是 | 媒体侧资源标识，编辑态不可修改 | R2 |
| BidURL | 输入框 | 是 | RTA 请求接口地址，URL 格式校验 | R2 |
| 媒体账户 | 选择器多选 | 是 | 至少绑定一个 | R2 |
| 状态 | 单选 | 是 | 上线 / 下线 | R2 |
| 配置 QPS | 数字 | 是 | 单 RTAID 请求上限 | R2 §9.5 |
| 实验已占用 QPS | 只读 | 否 | 启用状态实验占用总和，实时计算 | R2 §9.5 |
| 剩余 QPS | 只读 | 否 | 配置 QPS − 实验已占用 QPS | R2 §9.5 |
| SecretKey | 输入框 | 华为必填 | 华为签名校验；编辑态不明文回显 | R2 §9.4 |
| token | 输入框 | 小米必填 | 小米 token，与创意绑定 | R_connect §5.2 |
| 空设备 ID 参竞比例 | 数字 | 否 | 1%–100%，空设备 ID 请求参竞比例 | R1/R2 |
| 空设备 ID 出价系数 | 数字 | 否 | 不允许负数 | R1/R2 |
| 备注 | 多行文本 | 否 | 配置说明 | R2 |

状态与操作规则：

| 状态 | 支持操作 | 限制 |
| --- | --- | --- |
| 上线 | 查看、编辑、下线 | 必须配置 BidURL、媒体账户、QPS；华为必须配置 SecretKey |
| 下线 | 查看、编辑、上线、删除 | 存在实验中实验或启用中策略时不允许下线 |

删除限制：已被策略或实验引用的 RTAID 不允许删除；编辑被引用 RTAID 需提示风险。

### O4 RTAID 系统配置

| 字段 | 说明 | 来源 |
| --- | --- | --- |
| 平台分桶 ID 配置 | 分媒体维护（巨量/腾讯），至少 10 桶，同媒体桶 ID 唯一；仅对平台分桶生效，华为不读取 | R2 §10.4 |
| 系统 QPS 上限 | 租户/系统全局总额度，默认 300000；不可小于已分配 QPS | R2 §10.5 |

### O5 策略 Strategy

| 字段 | 说明 | 来源 |
| --- | --- | --- |
| 策略名称 | 同一媒体下唯一 | R2 |
| 内部策略 ID | 系统自动生成 | R1 |
| 媒体侧 RTA 策略 ID / RTAID | 必填，编辑态不可修改 | R2 |
| 适用媒体 | 编辑态不可修改 | R2 |
| 生效范围 | 巨量：账户/项目可配置；腾讯/华为：默认按 RTAID 绑定关系 | R2 §8.4 |
| 策略类型 | 实时策略 / 离线策略 / 全量参竞 | R1/R2 |
| 参竞策略 | 正向策略 / 反向策略（全量参竞展示“-”） | R1 |
| 出价方式 | 出价系数 / CPA 出价 / CPC 出价（正向策略时展示） | R1/R2 |
| 出价配置模式 | 固定出价 / 模型出价 | R2 §12.5 |
| 数据来源 | 数据库 + 数据表 + 分区号（可选）+ 二级分区（可选） | R2 §8.5 |
| 用户字段 | 多个，用于匹配和人群覆盖预估 | R2 §8.5 |
| 模型字段 | 仅 1 个，用于模型判断/模型出价 | R2 §8.5 |
| 覆盖人数 | 人群覆盖预估结果（约 XX 万人） | R2 §8.1 |
| 状态 | 草稿 / 已发布 / 已下线 / 已停用 | R2 §8.2 |

策略状态流转：

```text
草稿 ──发布──> 已发布 ──下线──> 已下线 ──发布──> 已发布
  │              │                    │
  └──删除        └──停用──> 已停用（不可恢复，需复制重建）
```

- 草稿：不可被实验引用；可查看、编辑、删除、发布、复制。
- 已发布：可被实验引用；不可直接编辑、删除；可下线、复制、查看数据报表。
- 已下线：不允许新增引用；可编辑、发布、停用、复制。
- 已停用：停止生效，关联实验同步失效；不允许新引用，不可恢复。

### O6 数据源与字段

| 字段 | 类型 | 必填 | 说明 | 来源 |
| --- | --- | --- | --- | --- |
| 数据库 | 下拉 | 是 | 系统维护的数据源 | R2 §8.5 |
| 数据表 | 下拉 | 是 | 策略使用的数据表 | R2 |
| 分区号 / 二级分区 | 输入框 | 否 | 定位数据分区 | R2 |
| 字段 ID | 输入框 | 是 | 英文唯一标识，用于匹配 | R2 |
| 字段名称 | 输入框 | 是 | 中文显示名，不作为匹配依据 | R2 |
| 字段描述 | 输入框 | 否 | 字段说明 | R2 |
| 数据类型 | 下拉 | 是 | integer、string 等 | R2 |
| 字段类型 | 单选 | 是 | 用户字段 / 模型字段 | R2 |

匹配逻辑：任一用户字段 ID 命中即具备进入策略判断条件；模型字段仅用于模型判断或出价。

### O7 实验 Experiment

| 字段 | 说明 | 来源 |
| --- | --- | --- |
| 实验名称 | 同一 RTAID 下不建议重复 | R2 |
| 实验类型 | 普通实验 / AB 实验 | R2 |
| 媒体 | 编辑态不可修改 | R2 |
| RTAID | 仅展示当前媒体下已上线且有权限的 RTAID；编辑态不可修改 | R2 |
| RTAID QPS 使用情况 | 选择 RTAID 后回显配置 QPS / 已占用 / 剩余 | R2 §11.2 |
| 实验描述 / 实验目标 | 非必填 | R2 |
| 分桶方式（AB） | 平台分桶 / 客户分桶（华为等新媒体仅客户分桶） | R2 §11.3 |
| 生效机制 | 立即生效 / 定时生效 | R1 §发布时间 |
| 生效时段 | 最多 5 条，不可重叠；开始早于结束 | R1 |
| 实验结束时间 | 可选 | R1 |
| 状态 | 草稿 / 实验中 / 已暂停 / 已结束 | R2 §11.1 |

实验状态与操作：

| 状态 | 支持操作 | 限制 |
| --- | --- | --- |
| 草稿 | 查看、编辑、删除、发布、复制 | 无 |
| 实验中 | 暂停、结束、复制、查看数据报表 | 不可修改分桶配置、策略组合、发布时间 |
| 已暂停 | 编辑、恢复、结束、复制、查看数据报表 | 编辑后需重新满足发布校验 |
| 已结束 | 查看、复制、编辑基础信息、重新启用、查看数据报表 | 不可修改策略配置、分桶配置、历史生效配置 |

发布前固定检查项（可复用为诊断规则）：

| 检查项 | 规则 | 来源 |
| --- | --- | --- |
| 出价系数范围 | 腾讯 0.2–5.0 合法，超出告警；负数阻断 | R1 |
| 出价过低/过高 | 巨量/腾讯 CPA/CPC 建议范围 100–10000，低于/高于告警，负数阻断 | R1 |
| 超过 QPS 上限 | 实验预计占用 QPS > RTAID 配置上限阻断；未配置 QPS 告警 | R1 |
| 实验间生效时间冲突 | 同 RTAID 下生效时间重叠告警（不阻断） | R1 |

### O8 实验组与分桶

| 配置项 | 规则 | 来源 |
| --- | --- | --- |
| 默认实验组 | 1 对照组 + 1 实验组 | R2 §11.5 |
| 对照组 | 不可删除 | R2 |
| 分桶数量 | 固定 10 桶，每桶默认 10% 流量 | R2 |
| 分配规则 | 每组至少 1 桶；同桶不可重复分配；总占比 ≤ 100% | R2 |
| 发布后修改 | 发布后不可直接修改分桶 | R2 |
| 平台分桶兜底 | 媒体未传桶 ID / 桶 ID 为空 / 不匹配 / 格式异常 → 执行对照组策略 | R2 §11.5 |
| 客户分桶兜底 | 匹配异常 → 对照组；计算失败 → 对照组并记录异常 | R2 |
| 空设备 ID 兜底 | 无设备 ID、客户分桶字段全空 → 执行 RTAID 兜底策略 | R2 §9.6 |

### O9 策略组 StrategyGroup

| 配置项 | 规则 | 来源 |
| --- | --- | --- |
| 组内逻辑关系 | 或（OR）；组间默认 OR | R2 §11.6 |
| 组内策略 | 至少 1 条；组内不可重复 | R1/R2 |
| 出价归属 | 组内关系 OR 时每条策略单独出价；AND 时策略组层级统一出价 | R2 |
| AB 实验约束 | 对照组与每个实验组至少 1 个策略组 | R2 |
| 发布约束 | 分桶未配置或策略组未配置时不允许发布 | R2 |

### O10 执行日志 ExecutionLog

| 字段 | 说明 | 来源 |
| --- | --- | --- |
| RTA 请求 ID / Trace ID | 单次请求唯一标识 | R1 |
| 请求时间 | 请求进入系统时间 | R1 |
| 媒体 / RTAID / 关联媒体账户 | 归属信息 | R1 |
| 用户标识 / 设备 ID 状态 | 手机号/OAID/IDFA/CAID/IMEI 等；正常 / 空设备 ID | R1 |
| 实验匹配信息 | 实验名称/ID/类型/状态/实验组/分桶 | R1 |
| 策略执行链路 | 策略组、组内逻辑、策略名称、内部/媒体侧策略 ID、类型、参竞策略、执行结果、是否命中、是否短路、执行耗时 | R1 |
| 出价执行信息 | 出价方式、配置模式、价格类型、模型字段/分值/命中区间、最终出价 | R1 |
| 兜底执行信息 | 是否触发异常兜底/空设备 ID 兜底、兜底动作、兜底原因 | R1 |
| 响应信息 | 最终决策（参竞/拒绝/跳过）、响应状态、响应耗时、失败原因 | R1 |

执行状态枚举：

| 状态 | 说明 |
| --- | --- |
| 执行成功 | 完整完成实验匹配、策略执行、结果返回，含正常返回参竞/拒绝/出价 |
| 兜底执行 | 触发兜底逻辑但兜底正常完成（策略异常/模型异常/空设备 ID/分桶缺失） |
| 执行失败 | 未进入完整决策流程，含以下失败原因 |

执行失败原因枚举（R1）：

| 失败原因 | 说明 |
| --- | --- |
| 系统异常 | 限流（超出 QPS 处理上限）等 |
| RTA 传入参数异常 | 媒体侧参数缺失或格式错误，无法识别 RTAID/用户标识/实验信息 |
| 媒体侧请求异常 | 请求结构异常、字段缺失或格式不符 |
| 模型异常 | 模型字段无效、模型分未匹配或区间无法匹配 |
| 未知异常 | 无法归类的其他异常 |

### O11 报表指标 Metrics

**资源口径（核心指标区）** —— 不受策略/实验组/分桶/出价等分析筛选影响：

| 指标 | 口径 | 告警阈值（R1 监控规则） |
| --- | --- | --- |
| 当前 QPS | 当前分钟平均每秒请求数 | QPS 使用率 > 80% 黄色风险；> 100% 红色高危 |
| 峰值 QPS | 筛选周期内最高 QPS | - |
| QPS 使用率 | 当前 QPS / 配置 QPS 上限（默认 30w） | 同上 |
| 平均响应耗时 | 周期内平均 | > 50ms 告警 |
| P95 / P99 响应耗时 | 分位耗时 | P99 > 100ms 告警 |
| 成功率 | 执行成功数 / 媒体请求总量 | < 95% 告警 |
| 失败率 | 执行失败数 / 媒体请求总量 | - |
| 超时率 | 超时请求占比 | > 3% 告警 |

**分析口径（使用情况指标区）** —— 支持策略/实验/分桶/出价下钻：

| 指标 | 口径 | 告警阈值 |
| --- | --- | --- |
| 请求总量 | 媒体侧进入 RTA 链路的请求总数 | 请求总量为 0 时比例类指标展示“-” |
| 执行成功数 / 失败数 | 按执行结果聚合 | 失败数环比明显升高提示风险 |
| 兜底执行数 | 异常兜底 + 空设备 ID 兜底 | 兜底占比异常升高提示 |
| 参竞数 / 参竞率 | 参竞数 / 请求总量 | 参竞率偏离正常范围（< 80%）告警 |
| 拒绝数 / 拒绝率 | 拒绝数 / 请求总量 | - |
| 策略命中数 / 命中率 | 命中当前策略的请求数占比 | 命中率 < 50% 告警 |
| 平均出价系数 / 平均 CPA 出价 / 平均 CPC 出价 | 按出价方式对应展示 | - |
| 模型出价命中数 / 固定出价命中数 | 按出价配置模式 | - |

**报表维度（明细数据区）**：RTAID、媒体、关联媒体账户、RTA 策略名称/ID、媒体侧 RTA 策略 ID、策略类型、参竞策略、RTA 实验名称/ID、实验类型、实验组、分桶、出价方式、出价配置模式、异常兜底。

---

## 3. 关键业务规则汇总（诊断相关）

| 规则 | 内容 | 来源 |
| --- | --- | --- |
| R1-限流 | 先按所有 RTAID QPS 总和限流，再循环处理各 RTAID，限流的直接返回异常 | R_connect |
| R2-响应时限 | RTA 接口响应需在 20ms 内，完整决策流程 100ms 内 | R1 |
| R3-QPS 计算 | 剩余 QPS = 配置 QPS − 实验已占用 QPS；剩余 < 0 不允许保存/发布 | R2 §9.5 |
| R4-多实验命中 | 同一用户命中多个实验时，执行最近一条 | R1 |
| R5-编辑限制 | 策略与实验编辑态均不支持修改 RTAID 与媒体 | R1/R2 |
| R6-出价互斥 | 出价系数/CPA/CPC 只能选一种；VIVO 同 put_in_type 不可同时配 rta_bid 与 boost_coef；荣耀同目标不可同时配 priceRate 与 priceInfo/convPriceInfo | R_connect §18 |
| R7-覆盖人数 | 仅统计用户字段，按用户维度去重，不包含模型字段 | R2 §8.5 |

---

## 4. 与 Demo Mock 数据的对应关系

| 对象模型 | demo `src/data/mock.ts` | 一致性 |
| --- | --- | --- |
| 媒体账户 O2 | account（M-2001 夏日电商品牌主账户） | ✅ |
| 项目（Demo 扩展对象，非 RTA 核心对象） | project（P-2026-SUMMER，日预算 1000/消耗 300） | ✅ |
| RTAID O3 | rtaBinding（juliang-rta-2086，QPS 300000/42800） | ✅ 与系统 QPS 默认值 30w 一致 |
| 实验 O7 + 实验组 O8 | experiment（EXP-2026-08，对照组 G-CONTROL 桶 1/2/5/6/9，实验组 G-TREAT-A 桶 3/4/7/8/10） | ✅ 10 桶分配符合规则 |
| 策略 O5 | 旧策略/新策略（准入门槛 40% → 80%） | ✅ 准入门槛为 Demo 抽象字段【待核验】 |
| 策略组 O9 | controlGroup/treatmentGroups 下 strategyGroupName | ✅ |
| 执行日志 O10 | technical.logFieldPath / 证据时间线 | ✅ 部分抽象 |
| 报表指标 O11 | causeTree 各节点 metrics | ✅ |

> 说明：Demo 中的「项目 Project」是投放管理层扩展对象（预算/消耗/达成率），用于表达预算未达标等业务现象；对象模型 O1-O11 覆盖其底层 RTA 域，两者通过 RTAID 与媒体账户关联。

---

## 5. 待核验清单

1. 巨量 RTA 报表请求字段/返回字段与指标口径（Kiki 后续补充截图核验）。
2. 准入门槛（模型打分阈值）在真实系统中的字段名与口径。
3. 项目（Project）与 RTAID/策略/实验的真实绑定关系字段。
4. 媒体侧出价字段换算（VIVO 毫分→元、荣耀元、OPPO price_ratio 范围）在 mock 中的体现方式。
5. 预算/消耗/达成率数据的真实来源表。
