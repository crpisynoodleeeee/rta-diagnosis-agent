# 项目三 · 部署与发布说明（D1）

> 目标：把 `demo/v6` 部署成简历可点的**在线 Demo**，附国内镜像与二维码。
> 本文档是"部署说明书"，按顺序执行即可；标 ⚙️ 的步骤需要你的账号操作（本环境无法代办）。

---

## 1. 先决条件（一次性）

- 一个 GitHub 账号（免费）；建议再注册 Gitee（码云）用于国内镜像（需实名认证）。
- 本机已装 Git、PowerShell；`deploy-ghpages.ps1` 就在仓库根目录。

---

## 2. GitHub Pages 部署（主站）

### 2.1 ⚙️ 创建 GitHub 仓库

在 GitHub 网页新建仓库（Public），仓库名建议 **`rta-diagnosis-agent`**（比 `program3-r` 更适合对外）。

### 2.2 关联远端

```powershell
cd D:\AI_Codex\Projects\program3-r
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
```

### 2.3 推送主分支（可选，建议做：让文档也在线上）

> ⚠️ 推送前先看 §4「敏感内容自查」，尤其确认 `R (2)/` 目录是否要公开。

```powershell
git push -u origin main
```

### 2.4 ⚙️ 一键部署 Demo 到 gh-pages

```powershell
.\deploy-ghpages.ps1
```

脚本做的事：创建/更新 `gh-pages` 分支，把 `demo/v6/` 内容放到分支根目录，推送到远端。

### 2.5 ⚙️ 开启 Pages（网页操作，1 分钟）

1. GitHub 仓库 → **Settings** → 左侧 **Pages**；
2. **Source** → `Deploy from a branch` → 分支选 **`gh-pages`**、目录 **`/ (root)`** → Save；
3. 等 1-2 分钟构建完成，页面顶部会出现成品地址。

**成品 URL = `https://<你的用户名>.github.io/<仓库名>/`**（gh-pages 分支根目录的 `index.html` 即 Demo）。

### 2.6 验收

- 手机/无痕浏览器打开成品 URL：列表、筛选、QPS 卡正常；
- 点第 1 行「发起诊断」→ 抽屉七段式正常、无 JS 报错（页顶无红框）；
- 左侧「智能参谋」问答正常；
- 控制台 Network 无红色失败请求（模板模式零外网，应只有本页资源）。

---

## 3. 国内镜像

GitHub Pages 国内访问不稳定，简历二维码建议**两个链接都放**（主 + 镜像）。

### 3.1 方式 A：Gitee Pages（推荐，免费）

```powershell
# ⚙️ Gitee 网页新建同名仓库（Public）
git remote add gitee https://gitee.com/<你的用户名>/<仓库名>.git
git push gitee main gh-pages
```

然后 Gitee 仓库 → **服务 → Gitee Pages**（首次需实名认证）→ 部署分支选 `gh-pages`、目录 `/` → 启动。

成品 URL = `https://<你的用户名>.gitee.io/<仓库名>/`

> 注意：Gitee Pages 服务偶有调整，若开通失败，用方式 B 兜底。

### 3.2 方式 B：Cloudflare Pages（免费，备用）

1. ⚙️ 登录 [Cloudflare](https://dash.cloudflare.com) → **Workers & Pages → Create → Pages**；
2. 选 **Direct Upload**，把 `demo/v6` 目录内容直接拖入上传 → 部署；
3. 每次更新重新上传即可（demo 是静态文件，无构建步骤）。

成品 URL = `https://<随机名>.pages.dev`

---

## 4. 敏感内容自查（发布前必读）

| 检查项 | 说明 |
| --- | --- |
| **真实平台截图** | `R (2)/` 目录含真实投放平台截图（企业微信截图），**建议不推送到公开仓库**。若仓库已公开且包含它们，最稳妥是新建公开仓库只放 `demo/v6` + 文档，不放 `R (2)/` |
| **API Key** | 前端零 Key（已由 `_verify_llm.cjs` 静态扫描保证）；本地环境变量不提交；发布前全仓库搜 `sk-` 确认无真实 Key |
| **个人信息** | `.claude/`、`.workbuddy/` 为工具配置/会话记忆，`.gitignore` 已忽略 `.workbuddy/`；如介意 `.claude/settings.json` 可加入 `.gitignore` |
| **README 占位符** | 仓库根 `README.md` 中的 Demo 链接是占位符，部署后替换为真实 URL |

---

## 5. 二维码生成（简历用）

成品 URL 确定后，任选其一：

**方式 A（最快，无需装东西）**：浏览器打开

```
https://api.qrserver.com/v1/create-qr-code/?size=400x400&data=<你的成品URL>
```

右键图片另存为 `作品集Demo二维码.png` 即可。

**方式 B（本地生成）**：

```bash
npx --yes qrcode -o qr.png "<你的成品URL>" -w 400
```

> 二维码生成后**不要再改 URL**（换域名/仓库名会让简历上的码失效）。GitHub 用户名不可改，仓库名定好后固定。

---

## 6. 部署后 Checklist

- [ ] 成品 URL 在手机 + 电脑均能打开、无 JS 报错
- [ ] 简历（PDF）替换为含二维码 + 两个链接的新版
- [ ] 根 README 的 Demo 链接占位符已替换
- [ ] `R (2)/` 未进入公开仓库
- [ ] 4 组验证脚本发布前重跑全绿（`cd demo/v6 && node _verify.cjs` 等）

---

_2026-08 · 项目三部署发布说明 v0.1_