# 项目三 Handoff 交接文档

> 项目：RTA 投放诊断 Agent  
> 交接日期：2026-08-21  
> 当前阶段：可演示原型已完成，进入作品集收口与质量修订阶段  
> 项目根目录：`D:\AI_Codex\Projects\program3-r`

---

## 1. 新对话接手指令

请先完整读取本文档，再读取第 8 节列出的核心文件。不要从零重新定义项目，也不要默认修改文件。

当前任务主线是：

1. 以 `demo/v6` 为唯一正式 Demo 版本；
2. 修复数据口径、评测脚本和 AI 接入说明中的硬问题；
3. 完成最终视觉验收、作品集包装和演示材料；
4. 保留 Agent 只诊断与生成草稿、人工确认和执行的产品边界。

除非用户明确要求，不要继续扩展新场景，也不要重做产品形态。

---

## 2. 项目定位

项目三是嵌入广告投放平台的 **RTA 投放诊断 Agent**。

一句话定位：

> 面向 RTA 投放优化的受控 Agentic Workflow，通过规则引擎、场景识别和 AI 归因解释定位投放异常，并生成可验证、可回滚的实验草稿，由人工确认后执行。

首要用户是投放优化师；管理者查看管理摘要；技术人员按需展开技术证据。

项目目标不是建设真实媒体系统，而是作为秋招 AI 产品作品集，展示：

- 对复杂业务链路的理解；
- Agent 产品形态和工作流设计；
- 规则与 AI 的职责划分；
- 人工确认、执行边界和安全护栏；
- Mock、Golden Case 和评测 Harness 的设计能力。

---

## 3. 已确定的核心产品决策

### 3.1 产品形态

采用：

```text
投放管理 > RTA > RTAID 配置列表 > 发起诊断 > 右侧诊断抽屉
```

不是独立 AI 工作台，不是聊天机器人，也不是营销型 Dashboard。

主页面沿用原投放平台的顶部导航、左侧菜单、筛选区和高密度表格；诊断结果在右侧抽屉中完成查看和操作。

### 3.2 Agent 工作流

```text
用户选择对象与日期
→ 发起诊断
→ 读取数据
→ 规则判定
→ 场景识别
→ AI 归因、解释与规划
→ 人工确认
→ 人工执行真实配置
→ 等待数据回流
→ 复盘
```

### 3.3 职责边界

- 规则引擎负责确定性指标计算和异常判定；
- 场景识别负责判断异常卡在哪个链路阶段；
- AI 负责归因、分角色解释和生成实验方案；
- AI 不自行计算核心指标，不覆盖规则判定；
- AI 不直接修改预算、出价、策略或真实媒体配置；
- 所有真实配置变更由人工确认和执行；
- 建议必须包含观察指标、成功标准和回滚条件。

### 3.4 标准输出结构

诊断抽屉采用七段式输出：

1. 诊断结论；
2. 影响概览；
3. 原因排序；
4. 证据时间线；
5. 原因树；
6. 建议方案；
7. 人工确认与技术详情。

另有 `00 · 诊断场景`，用于展示 S0-S7 场景、现象标签和置信度。

---

## 4. 业务对象与链路

### 4.1 投放层级

```text
媒体
→ 媒体账户
→ 项目
→ 投放单元
→ 创意
```

当前 Golden Case 使用一个媒体、一个账户、一个项目、两个单元、两个创意。

### 4.2 RTA 层级

```text
媒体侧 RTA ID
→ 平台内生效实验
→ 对照组 / 实验组
→ 分桶
→ 对应策略
→ 单次请求的参竞与出价决策
```

注意：RTA ID 是媒体识别和调用的统一入口，不等于某一套固定策略。具体绑定在项目层还是单元层，仍需通过巨量真实字段截图确认。

### 4.3 数据链路

```text
媒体请求 RTA 接口
→ 平台识别 RTA ID、实验和分组
→ 执行策略
→ 返回参竞与出价建议
→ 媒体完成竞价
→ 曝光、点击、消耗数据回流
→ 转化归因
→ 按实验组汇总效果
```

策略命中不等于竞价获胜。曝光、消耗和转化必须依赖后续媒体报表与归因数据。

---

## 5. Golden Case

项目：`P-2026-SUMMER 夏季大促`。

### 5.1 基础数据

- 媒体：巨量；
- 一个广告账户；
- 一个项目；
- 两个投放单元；
- 两个创意；
- 一个 RTA ID；
- 一个实验；
- 对照组和实验组各 50% 流量；
- 日预算：1,000 元；
- 实际消耗：300 元；
- 对照组消耗：250 元；
- 实验组消耗：50 元。

### 5.2 异常证据

- 媒体请求量正常；
- RTA 服务无明显超时；
- 11:50 实验组准入门槛由 40% 调整为 80%；
- 12:00 配置生效；
- 实验组参竞率由 60% 降至 10%；
- 曝光和消耗随后下降；
- 实验组 CPA 不差，因此不能只为消耗盲目放宽。

### 5.3 标准诊断

> 实验组准入门槛过严，导致参竞率下降，进一步导致曝光和消耗下降，最终造成日预算未达标。

### 5.4 标准建议

- 门槛从 80% 调整至 60%；
- 先用小流量运行 24 小时；
- 观察参竞率、消耗、预算达成率和 CPA；
- 成功标准：参竞率至少恢复到 50%，预算达成率至少达到 80%；
- 回滚条件：CPA 上涨超过 20% 或流量质量显著下降；
- 人工确认后再执行。

---

## 6. 当前 Demo 状态

当前唯一正式版本：

`D:\AI_Codex\Projects\program3-r\demo\v6\index.html`

`v1-v5` 均视为历史或废弃版本，不应再基于它们开发。

### 6.1 已实现

- 投放平台顶部导航和左侧 RTA 菜单；
- RTAID 配置列表；
- QPS 指标概览；
- 搜索和多维筛选；
- 10 条覆盖 S0-S7 的 Mock 数据；
- 预算未达标、CPA 过高、可安全放量等现象标签；
- 右侧 720px 诊断抽屉；
- 七段式诊断结果；
- 管理摘要、运营解释和技术详情三层表达；
- 原因树和证据时间线；
- 实验建议、成功标准、回滚条件；
- 人工批准、修改、拒绝、已执行、等待回流、复盘；
- 数据不足和诊断失败状态；
- 模板输出与 LLM 失败回退；
- 完全离线展示能力。

### 6.2 已实测

2026-08-21 已完成以下实测：

- 浏览器打开主页面成功；
- 列表和筛选区正常渲染；
- Golden Case 点击“发起诊断”成功；
- 诊断结果进入“待人工确认”；
- 点击“批准方案”后进入“待人工执行”；
- 页面无 JavaScript error；
- 存在一条 Tailwind 非生产环境 warning，不影响 Demo 操作。

### 6.3 自动验证

在 `demo/v6` 下运行：

```powershell
node _verify.cjs
node _verify_scenes.cjs
node _verify_llm.cjs
```

实测结果：

- Golden Case 与 HTML 合规检查：全部通过；
- 5 个扩展场景叙事完整性：全部通过；
- LLM 关闭、成功、网络错误、HTTP 500、错误 JSON、输出不合规：6/6 通过。

---

## 7. 当前遗留问题与优先级

### P0：QPS 数据口径矛盾

页面目前同时显示：

- 系统上限：300,000；
- 已分配：1,120,000；
- 已使用：443,800；
- 剩余：676,200；
- 使用率：39.6%。

后四项是按 10 个 RTAID 的配置 QPS 聚合，第一项像单个系统或单 RTAID 上限，口径不一致。必须统一文案和计算方式，否则面试时容易被直接质疑。

建议二选一：

1. 将 300,000 改成与聚合口径一致的全局 QPS 容量；
2. 将“系统上限”改为“单 RTAID 配置上限”，并与聚合指标分开展示。

### P0：真实 LLM 接入尚未完成最终验收

- 默认模式走模板，适合稳定演示；
- `_verify_llm.cjs` 只是 stub 测试，不是真实 DeepSeek 请求；
- 前端仍要求存在 `LLM_CONFIG.apiKey` 才能开启 AI 模式；
- 本地 `llm-proxy.mjs` 仍从前端 Authorization 接收 Key；
- README、源码注释和代理方案存在描述不完全一致的问题。

作品集演示可继续默认走模板，但对外表述必须诚实：真实 LLM 接口已有形状和兜底验证，尚未作为生产级后端接入。

若继续真实接入，Key 应放服务端环境变量，前端不得保存或发送用户可见 Key 配置。

### P1：场景验证脚本漏检影响范围

`_verify_scenes.cjs` 打印 `a.affectedScope`，而真实报告中的位置是 `rpt.impact.affectedScope`，因此控制台显示 `undefined`，但测试仍通过。

应修正读取路径，并增加“影响范围非空”断言。

### P1：版本和 Git 状态未收口

- 大量文档、截图、`demo/v2-v6` 尚未提交；
- 只有一次初始提交；
- 应明确 `v6` 为正式版本；
- 旧版本可保留，但需在 README 中清楚标记废弃；
- 完成修订后再形成一次有意义的提交。

### P1：最终作品集验收尚未完成

还需要：

- 统一核心数字和业务文案；
- 检查 1440px 桌面视口下的抽屉和表格；
- 录制 60-90 秒 Golden Case 演示；
- 将产品问题、决策、Agent 架构、Demo 和评测结果整理成作品集叙事；
- 准备面试追问：为什么需要 AI、为什么不用监控告警、如何防止 AI 乱改配置。

### 辅助蓝图遗留

`program3-r-01` 是更广义“智能投放数据交互”蓝图，不是当前 Demo 主线。它仍记录以下问题：

- 标题和内容不一致；
- 缺“场景 × 执行边界 × 载体”汇总矩阵；
- 场景三的自动执行与人工确认时序冲突；
- 统一数据管道未同步执行边界；
- 感知与建议之间缺诊断归因层。

不要让这些辅助蓝图问题阻塞项目三 Demo 收口；若用于作品集，则需先修复或只选取已成熟部分。

---

## 8. 核心文件阅读顺序

新对话建议按以下顺序读取：

1. `D:\AI_Codex\Projects\program3-r\项目三-Handoff交接文档-2026-08-21.md`
2. `D:\AI_Codex\Projects\program3-r\PRD v0.2.1.md`
3. `D:\AI_Codex\Projects\program3-r\Agent输入输出结构契约 v0.3.md`
4. `D:\AI_Codex\Projects\program3-r\AI层设计与评测方案.md`
5. `D:\AI_Codex\Projects\program3-r\MVP诊断规则表.md`
6. `D:\AI_Codex\Projects\program3-r\Mock数据口径说明.md`
7. `D:\AI_Codex\Projects\program3-r\场景扩展方案.md`
8. `D:\AI_Codex\Projects\program3-r\demo\v6\README.md`
9. `D:\AI_Codex\Projects\program3-r\demo\v6\index.html`
10. `D:\AI_Codex\Projects\program3-r\作品集五段式整理.md`
11. `D:\AI_Codex\Projects\program3-r\项目三面试速记卡.md`

参考截图目录：

`D:\AI_Codex\Projects\program3-r\R (2)\R\PICTURE`

关键截图：

- `企业微信截图_17866879325577.png`：RTAID 配置列表母版；
- `企业微信截图_17866867509516.png`：RTA 报表页面；
- `企业微信截图_17866865728109.png`：右侧抽屉母版；
- `企业微信截图_17866866619909.png`、`17866866905699.png`、`17866867055210.png`：实验配置步骤。

---

## 9. 推荐的下一轮工作顺序

### 第一轮：修硬问题

1. 修正 QPS 总量口径；
2. 修正 `_verify_scenes.cjs` 的影响范围断言；
3. 统一 README、前端和代理的 LLM 接入说明；
4. 重新运行三组验证；
5. 浏览器重新验收 Golden Case 和人工确认流程。

### 第二轮：作品集收口

1. 以 `v6` 生成最终演示截图；
2. 完成 60-90 秒演示脚本；
3. 更新作品集五段式材料；
4. 将评测结果转成作品集中的可视化表格；
5. 整理面试问答。

### 第三轮：可选增强

1. 使用服务端环境变量完成一次真实 DeepSeek 端到端调用；
2. 增加真实 LLM 与模板输出的对比评测；
3. 决定是否整理更广义智能投放蓝图。

---

## 10. 当前完成度判断

| 模块 | 完成度 | 判断 |
|---|---:|---|
| 业务框架与产品定义 | 95% | 主线稳定，不建议继续扩范围 |
| 规则与场景体系 | 95% | D1-D14、S0-S7 已落地 |
| 前端 Demo | 90% | 已可操作，需修数字和最终视觉细节 |
| AI 层与评测 | 80% | 模板与 stub 验证完整，真实调用未闭环 |
| 作品集包装 | 80% | 材料齐全，仍需最终串讲和截图 |
| 总体 | 约 85% | 已进入收口阶段 |

---

## 11. 可直接发送给新对话的开场提示

```text
请接手项目三“RTA 投放诊断 Agent”的收口工作。

先完整读取：
D:\AI_Codex\Projects\program3-r\项目三-Handoff交接文档-2026-08-21.md

项目根目录：
D:\AI_Codex\Projects\program3-r

当前正式 Demo：
D:\AI_Codex\Projects\program3-r\demo\v6\index.html

不要从零重新定义项目，不要继续扩展新场景，也不要基于 v1-v5 开发。

先检查 Handoff 中列出的 P0/P1 问题，并向我汇报你对当前状态和下一步的理解。在我确认前不要修改文件。
```

