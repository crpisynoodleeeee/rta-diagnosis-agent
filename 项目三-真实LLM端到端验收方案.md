# 项目三 · 真实 LLM 端到端验收方案 v0.1

> 目标：把"AI 价值增量"从**论证**升级为**实测证据**。
> 本文档回答两个问题：
> 1. 真实链路（浏览器前端 → 本地代理 → DeepSeek → 输出校验 → 回退）能不能跑通、且不泄露 Key；
> 2. 真实 LLM 在"归因 / 解释 / 规划"上是否**严格优于**模板兜底（即《AI 层设计与评测方案》§1 的"替代测试"论断是否成立）。
> 关联：《AI 层设计与评测方案 v0.1》《Agent 输入输出结构契约 v0.3》《demo/v6/README.md》。

---

## 1. 背景：为什么要做这次验收

当前状态（已核验）：

- `demo/v6` 的 4 组自动化验证全部是 **stub 测试**：`_verify_llm.cjs`（10 用例）与 `_verify_assistant.cjs`（47 用例）都用 `fetch` stub 伪造 DeepSeek 响应，**从未发起过真实 DeepSeek 网络请求**。
- 因此，目前"AI 做了规则引擎做不到的事"这一核心论断，只有**设计论证**，没有**实测证据**。
- 面试被追问"AI 到底带来了什么增量"时，现在只能答设计，拿不出数据。

本次验收的价值：产出可展示的**对照实验结果**（模板分 vs 真实 LLM 分），把作品集里"AI 价值"一句话从观点变成数据。

---

## 2. 验收范围（明确边界，防止目标漂移）

### 2.1 本次验收（范围 A：LLM 质量 + 链路）

| 项 | 是否在本次 |
| --- | --- |
| 诊断 LLM 通道 `callLLM`（真实 DeepSeek） | ✅ |
| 智能参谋 LLM 通道 `callAssistantLLM`（真实 DeepSeek） | ✅（第二阶段，见 §9） |
| 前端 prompt 构造 → 本地代理 → DeepSeek → 解析 → 5 项校验 → 回退 | ✅ |
| 模板兜底 `buildTemplateNarrative` 作为对照基线 | ✅ |
| Key 不泄露（前端无 Authorization / 无 apiKey） | ✅（复用现有静态+动态断言，真实侧交叉验证） |
| 浏览器 UI 渲染（角标"模板→AI 生成"） | ❌ 不在本次（见 §2.2） |
| CORS / 代理安全性 / 2MB body 上限 / 30s 超时 | ❌ 已由 `_proxy-test.sh` + 代码审阅覆盖，不重复 |

### 2.2 明确不做的

- **不做浏览器可视化自动化**（Playwright 点抽屉、看角标变化）。那是 UI 连通性验收，与"AI 质量"正交；已有的 1440px 人工可视化验收负责 UI 侧。
- **不把"AI 增量"偷换成"LLM 调用成功"**。调用成功 ≠ 质量好。核心结论必须来自**打分对比**，而不是"返回了非 null"。

---

## 3. 前置条件

### 3.1 补"标准答案卡片"（ground truth，本次验收的硬前提）

现状：只有 Golden Case（S3，juliang-rta-2086）有正式标准答案；其余 9 个 RTAID 只有模板叙事，**没有可打分的 golden label**。

前置动作：为 §5 的评测集逐条补标准答案卡片，每条含：

```text
场景 sceneId / 现象标签
主因（语义锚点 + 关键数值，如"门槛 40%→80%"）
因果链方向（不可反、不可跳）
次因（单主因必须"无独立次因"）
建议动作区间（如"门槛回调 60%，允许 55–65"）
三要素（观察指标 / 成功标准 / 回滚条件）
```

> 这是本次验收里**最耗人力的环节**，也是"评测不变成自欺"的关键。标准答案必须先于跑 LLM 冻结，且由人写、不由 LLM 生成。

### 3.2 环境与 Key

```bash
# 只在代理进程设置 Key（复用现有安全模型，harness 永不读 Key）
$env:DEEPSEEK_API_KEY = "sk-xxxxxx"
cd D:\AI_Codex\Projects\program3-r\demo\v6
node llm-proxy.mjs          # 仅绑 127.0.0.1:8787
```

- harness 通过 `http://127.0.0.1:8787/llm` 走代理，**不直连 DeepSeek**、**不读环境变量**。
- Key 内容不进日志、不进 git、不进报告。

### 3.3 成本预算（硬停）

DeepSeek chat 价格极低，本次规模约 50 次调用（10 场景 × 5 次重复），总成本预计 **< ¥2**。设硬停上限 **¥10**：harness 累计 token 超限即中止并报告。成本不是风险，但要留记录证明"可重复、不烧钱"。

---

## 4. 核心假设（验收要证伪/证实的命题）

| # | 命题 | 结论判定 |
| --- | --- | --- |
| H1 | 真实 LLM 在 L2 归因正确性上 **100% 命中**（一票否决项） | 任一场景 L2 失败 → 整体"不通过"，暴露 prompt/校验问题 |
| H2 | 真实 LLM 在 L3 解释、L4 规划上 **严格优于** 模板（增量 > 0） | 这是"AI 价值"的直接证据 |
| H3 | 真实 LLM 在 L2 上 **不劣于** 模板 | 若 LLM 比模板还差，说明当前接入是负资产 |
| H4 | 全链路无 Key 泄露、失败正确回退 | 复用现有安全断言 |

> 特别注意 H1/H3 是**反向信号**：如果真实 LLM 反而频繁被 5 项校验拦截、或归因跑偏，结论不是"失败丢人"，而是"发现当前 prompt / 校验阈值 / 能力边界"，属于高价值发现，需返修后复测。

---

## 5. 评测集设计

映射现有 10 条 `MOCK_RTA_LIST` 到 S0-S7，分三层：

### 5.1 主评测集（必过，1 条）

| RTAID | 场景 | 测什么 |
| --- | --- | --- |
| juliang-rta-2086 | S3 参竞/放行 | **Golden Case**：单主因、因果链、建议 80→60，是 L2 一票否决的锚点 |

### 5.2 扩展评测集（增量证明，7 条）

| RTAID | 场景 | 测什么 |
| --- | --- | --- |
| tencent-rta-3112 | S0 配置故障（QPS 超限） | 多异常类型 + CPA 过高标签 |
| juliang-rta-3050 | S0 配置故障（账户未绑定） | 配置类故障归因 |
| juliang-rta-2099 | S1 入口流量 | **"问题在媒体流量、不在 RTA"**——AI 是否敢于说"不调 RTA 策略" |
| xiaomi-rta-7701 | S2 命中率下降 | 多建议（人群回滚 + 字段放宽） |
| honor-rta-1245 | S4 出价/竞价 | 多建议（出价回调 + 小流量试探） |
| vivo-rta-8810 | S5 执行质量 | 先排障、不调 RTA |
| oppo-rta-9921 | S6 回传/归因 | 回传异常 → 效果可信度（CPA 虚高） |

### 5.3 边界评测集（诚实降级，2 条）

| 场景 | 测什么 |
| --- | --- |
| 数据不足（勾选演示开关） | AI 是否诚实输出"数据不足"，**不编造**主因/建议 |
| S7 无链路异常（huawei-rta-5040 / tencent-rta-4080） | 正常态：给出"可安全放量"而非硬造异常 |

> 这 10 条正好覆盖《AI 层设计与评测方案》§6 列出的四类待扩展边界：多主因（用 S0 近似）、数据不足、媒体流量（S1）、回传异常（S6）。其中"严格多主因（门槛+出价同时异常）"当前 mock 无现成样本，若需覆盖可新增一条 mock 或标注为"后续"。

---

## 6. 对照基线设计（关键：同尺子双跑）

每个评测样本，**同一份输入、同一把尺子，跑两条路径**：

```text
输入：record + signals + scenario（规则引擎输出，完全一致）
├─ 基线   buildTemplateNarrative(record, signals, scenario)  → 模板输出
└─ 实验组 await callLLM(record, signals, scenario)           → 真实 LLM 输出（或 null→回退模板）
```

- 若 `callLLM` 返回 `null`（被校验拦 / 网络错 / 超时），该样本的 LLM 输出 = 模板输出（回退语义），并在报告中单列"回退率"。
- **回退率本身是核心观测指标**：回退率过高说明接入还不稳，此时"增量"即使 >0 也不可信。

---

## 7. 评测维度与打分（复用 L1-L4，混合机检 + 人工）

### 7.1 L1 判定正确性（前置，不计入 AI 分）

- 规则引擎 D1-D14 判定 vs 标准，**必须 100% 一致**，否则该样本直接作废（判定错，归因必错）。
- 机检，脚本自动比对 `signals` / `scenario.sceneId`。

### 7.2 L2 归因正确性（一票否决）

| 指标 | 判定方式 |
| --- | --- |
| 主因命中 | 机检硬锚点（数值如 40%→80%、60%→10%）+ 人工语义兜底 |
| 因果链方向 | 机检关键词顺序（"门槛→参竞→曝光→消耗"方向）|
| 单主因"无独立次因" | 机检字符串 + 人工确认"次因不是硬凑" |

> L2 任一硬性项不命中 → 该样本整体不合格，L3/L4 得分作废（沿用《AI 层方案》§5.3）。

### 7.3 L3 解释质量 / L4 规划质量（打分 0-1）

沿用《AI 层方案》§5.3 的打分表，但明确**谁来打分**：

- **机检层（客观、可复现）**：管理摘要三要素齐全、建议三要素齐全、建议数值落在区间、术语是否出现。脚本自动算，作为"下限分"。
- **人工双盲层（语义、一锤定音）**：分角色分层（摘要≠运营≠技术，非复制三遍）、术语翻译准确、可执行性（有数值/时间窗/阈值）。至少 1 人盲评，模板与 LLM 输出**打乱顺序匿名打分**，避免"知道哪个是 AI 就给高分"。
- **（可选）LLM-as-judge 交叉**：用**不同模型/provider** 的第二 LLM 打分，只作参考，不作为最终结论（防止"运动员当裁判"）。报告需同时给出 judge 分与人工分的一致性。

### 7.4 评分汇总

每个样本产出 4 个分数：`L2(通过/否)`、`L3_tpl / L4_tpl`（模板）、`L3_llm / L4_llm`（LLM）。增量 = `(L3_llm + L4_llm) - (L3_tpl + L4_tpl)`。

---

## 8. 通过 / 不通过标准（硬阈值，先定后跑）

| 判定 | 阈值 |
| --- | --- |
| ✅ 链路通过 | 主集 100% 请求成功或按设计回退；全链路 0 次 Key 泄露（前端 headers 无 Authorization） |
| ✅ 价值通过 | ① 所有样本 L2 = 通过（含数据不足样本正确降级）；② 每样本 L3_llm≥0.7 且 L4_llm≥0.7；③ 汇总增量 `mean(L3+L4 增量) > 0`，且开放维度（分层/翻译/可执行性）出现**可引用的具体改进** |
| ⚠️ 待改进 | L2 全过，但部分样本 L3/L4 < 0.7 或增量 ≈ 0 |
| ❌ 不通过 | 任一非边界样本 L2 失败，或出现数据不足仍编造，或增量 < 0 |

> "价值通过"必须能落到**一句可展示的话**上，例如："在 10 场景 × 5 次重复中，真实 LLM 的归因命中率 100%，解释/规划质量分比模板高 X 分，且所有执行类/超范围请求均正确拒绝。"

---

## 9. 稳定性设计（消除"一次运气"）

- 每样本重复 **N=5 次**（`temperature=0.4` 下仍有随机性），报告 `命中率` + `分数均值±方差`。
- 单样本 L2 通过率 < 100% 即记录为"不稳定"，需在报告标注（这本身就是结论：当前 prompt 不够稳）。
- 成本换算：10 样本 × 5 次 = 50 次诊断调用，加智能参谋通道约 20 次，总计 < 100 次，成本仍 < ¥5。

---

## 10. 实施步骤（落到具体文件）

### 10.1 新增文件（全部在 `demo/v6/` 下，不破坏现有 stub 测试）

| 文件 | 作用 |
| --- | --- |
| `_eval_cases.json` | 10 条评测集 + 标准答案卡片（冻结后跑） |
| `_verify_llm_e2e.cjs` | 真实链路 harness：复用 `_verify_llm.cjs` 的 vm 加载手法，把 stub `fetch` 换成 `globalThis.fetch` 指向 `http://127.0.0.1:8787/llm`；调用 `buildTemplateNarrative` 与 `callLLM` 各取输出并落盘 |
| `_score_e2e.cjs` | 打分器：机检层自动算分 + 输出"人工双盲打分表"模板 |
| `acceptance/`（gitignore） | 原始 prompt / 原始响应 / 分数，**不进 git** |

### 10.2 执行顺序

```text
1. 冻结 _eval_cases.json（人工写标准答案，先于任何 LLM 调用）
2. 启动 llm-proxy.mjs（DEEPSEEK_API_KEY 已在环境变量）
3. node _verify_llm_e2e.cjs          # 跑 10×5，落盘原始输入输出
4. node _score_e2e.cjs               # 机检打分，生成人工盲评表
5. 人工双盲打分（可选 + LLM-as-judge 交叉）
6. 出报告，更新 README / 作品集五段式 / 面试问答库的"AI 价值"表述
```

### 10.3 智能参谋通道（第二阶段，复用同一 harness 思路）

- 用 `callAssistantLLM` / `classifyAssistantIntent` / `buildAssistantTemplateAnswer`，对 9 类意图 + 执行拒绝 + 超范围各跑真实 LLM，验证"只基于当前 RTA 事实、不编造、拒绝执行"。
- 打分标准同 §7，但 L2 换成"意图正确 + 不越界 + 不编造"。

---

## 11. 安全与合规

1. Key 只在代理进程环境变量；harness 不读、不打印、不落盘 Key。
2. `acceptance/` 加入 `.gitignore`，原始响应（可能含真实业务数据样例）不进仓库。
3. 复用现有静态+动态安全断言（前端无 apiKey / 无 Authorization / 无填 Key 提示），真实侧再交叉验证一次请求头。
4. 超时（前端 10s / 代理 30s）、限流、重试（同一请求不重复计费）、token 硬停（¥10 上限）。

---

## 12. 交付物清单

1. `_eval_cases.json`（评测集 + 标准答案，可复跑）
2. `_verify_llm_e2e.cjs` + `_score_e2e.cjs`（可复现的验收工具）
3. `项目三-真实LLM端到端验收报告-YYYYMMDD.md`（模板分 vs LLM 分、增量、回退率、结论判定表、原始证据索引）
4. 更新 `demo/v6/README.md` §7.7/§8：把"stub 测试"与"真实 E2E 已验收"区分写清
5. 更新 `作品集五段式整理.md` / `项目三面试问答库.md`："AI 价值"段落从"论证"升级为"实测数据"

---

## 13. 风险与应对

| 风险 | 应对 |
| --- | --- |
| 真实 LLM 频繁被 5 项校验/Golden 特判拦截 → 回退率高 | 这是**头号预期发现**：先记录回退率，再决定"放宽机检、保留语义判"还是"收紧 prompt"；复测后再下结论 |
| LLM 归因语义对但措辞不同 → 机检硬锚点误判 | 机检只做"数值/方向锚点"，语义命中交给人工双盲，不因措辞判负 |
| LLM-as-judge 自评偏差 | judge 用不同模型/provider，且只作参考；最终以人工双盲为准 |
| 温度随机性 → 单次运气通过 | N=5 重复 + 通过率 + 方差报告 |
| 成本/限流 | ¥10 硬停 + 小样本（50 次） |
| 数据不足样本 LLM 仍编造 | 作为 L2 一票否决项单列，编造 = 直接 ❌ |

---

## 14. 一句话结论判定

> 验收的产物不是"LLM 能调用"，而是**一张对照表**：同一批输入、同一把尺子下，真实 LLM 在归因上不输模板、在解释与规划上**可量化地优于**模板，且全程不泄 Key、失败正确回退。这张表落地的那一刻，"AI 价值增量"就不再是论证，而是证据。

---

_版本：v0.1 · 状态：待评审 · 下一步：先评审本方案，再补 `_eval_cases.json` 标准答案卡片（§5 前置动作）_