# 项目三：RTA 投放诊断 Agent · 面试问答库 v0.1

> 用途：求职作品集配套的面试准备材料。每个问题给「一句话回答」（30 秒内可讲完）+「展开要点」（1-2 分钟）+「证据点」（指向项目内可展示/可引用的材料）。
> 基于：产品定义 v0.1、场景扩展方案、AI 层设计与评测方案、PRD v0.2.1、契约 v0.2、真实业务沟通纪要。
> 提示：回答时优先讲"产品判断"和"取舍依据"，少讲术语堆砌；被追问时再用展开要点。

---

## 一、项目动机与定位

### Q1. 为什么做这个项目？它解决了什么问题？

**一句话**：广告投放优化师在"预算没跑出去"这类异常出现时，要在 RTAID 配置、账户绑定、策略工厂、实验、QPS、报表、执行日志、归因回传 7 个以上页面之间人工排查，难以快速判断异常发生在哪一层。我做的 Agent 把分散的多源信号合成一条可解释的判断链，定位根因并生成需人工确认的实验方案。

**展开要点**：
1. 真实痛点：不是"指标算不出来"，而是"判断路径太长"——异常可能卡在 8 个环节的任何一个，且各环节数据分散在不同页面。
2. 决策快 ≠ 排查快：RTA 单次请求的决策只要 100ms（毫秒级实时判断），但"今天预算为什么没跑出去"是天级的事后归因分析，两者不在一个时间尺度。系统越实时越自动，出问题后的排查越难。
3. 我的定位：受控 Agentic Workflow（human-in-the-loop），不是聊天 Copilot，也不是自动执行 Agent。

**证据点**：产品定义 §1-2；项目三框架定义 §2（核心问题）。

---

### Q2. 为什么选择"有预算但跑不出去"作为 MVP 场景？

**一句话**：它和 RTAID、QPS、策略状态、实验状态、执行日志最直接相关，RTA 资料依赖度最高、诊断链路最清晰，最适合做成可运行 Demo，也最容易解释 AI 价值。

**展开要点**：
1. 匹配度：这个场景贯穿 RTA 全链路（配置层 → 链路层 → 效果层），能最大化复用真实 PRD/截图资料。
2. 诊断路径清晰：12 步链路（D1-D14），适合结构化实现。
3. 可展示性：规则发现异常 → AI 综合上下文解释原因 → 生成实验方案，AI 价值一目了然。
4. 与后续场景互补：它是"花不出去"，和"花出去但贵（CPA 过高）"、"跑得好可放量"构成完整投放生命周期。

**证据点**：产品定义 §3；场景扩展方案 §2.3。

---

## 二、AI 价值与分工

### Q3. 这个 Agent 里，AI 到底做了什么？为什么要用 AI 而不是纯规则引擎？

**一句话**：规则引擎回答"哪个环节出了问题"（判定，有限状态可枚举），AI 回答"为什么出问题、影响多大、下一步怎么验证"（归因 + 解释 + 规划，开放空间的因果推理与生成）。

**展开要点**：
1. 替代测试：把 Agent 输出逐条过筛子"能不能用 if-else 写尽"——判定能（归规则引擎），归因/解释/规划不能（归 AI）。
2. 归因不是算出来的：规则引擎只能告诉你"参竞率下降"和"门槛从 40% 改到 80%"两个事实同时为真，但它不知道谁是因谁是果——因果方向是语义关系，不是阈值关系。
3. 解释是开放文本：同一份 causeTree 要生成"老板听的"和"技术听的"两套表达，if-else 写不尽。
4. 规划是开放空间：方案要结合业务上下文（大促/季节），不能穷举。
5. 一句话总结：**判定是有限状态的可枚举逻辑，归因和规划是开放空间的因果推理与生成。**

**证据点**：AI 层设计与评测方案 §1（核心论断）；demo6 中 runRules（规则层）与 runAiLayer（AI 层）显式分离。

---

### Q4. 规则引擎和 AI 是怎么分工的？为什么不全交给 AI？

**一句话**：三层分工——规则引擎做确定性硬判定和证据收集，场景识别按规则命中组合判断场景归属，AI 只做归因、解释、方案生成；AI 不计算指标、不伪造证据、不改变判定结果。

**展开要点**：
1. 分层：第 1 层规则引擎（D1-D14，确定性，全场景复用）→ 第 2 层场景识别（S0-S7，卡点定位 + 现象标注）→ 第 3 层 AI（归因/解释/规划）。
2. 为什么不全交给 AI：硬判定（RTAID 是否上线、QPS 是否超限）是确定性的，交给 AI 反而引入幻觉风险；规则引擎保证"判定可复现、可审计、不胡说"。
3. 关键边界：AI 层只消费规则引擎的结构化输出（signals + scenario），不自己查数、不算指标——这是"AI 不乱说"的结构性保证。
4. 工程意义：规则引擎可独立替换为真实 API，AI 层可独立替换为真实 LLM，两者解耦。

**证据点**：PRD §9 三层分工表；场景扩展方案 §1（三层架构图）。

---

### Q5. 怎么保证 AI 不乱说、不编造？(human-in-the-loop 的可靠性设计)

**一句话**：四道防线——① AI 只消费规则引擎的结构化事实，不算数不查数；② 数据不足时强制输出"数据不足"状态，不强行诊断；③ 所有配置变更必须人工审批后执行（AI 只生成草稿）；④ 有 Golden Case 标准答案评测 AI 输出质量。

**展开要点**：
1. 结构性防线：AI 输入是 causeTree + timeline + configChanges（规则引擎已核实的事实），AI 无权访问原始数据、无权改判定。
2. 诚实性防线：契约 §2.4——关键字段缺失时输出 insufficient，不编造指标值；这是"Agent 不伪造"原则的落地。
3. 执行边界防线：AI 只生成实验草稿，批准后也只是"待执行草稿"，修改真实配置必须人工在策略工厂/RTA 实验页面操作。
4. 评测防线：Golden Case 标准答案（主因/因果链/建议动作/三要素），AI 输出与之做语义一致性比对（L2 一票否决）。

**证据点**：AI 层设计与评测方案 §5（评测标准）；契约 v0.2 §2.4（数据不足）；demo6 的 Agent 边界声明 + 错误捕获。

---

## 三、产品设计

### Q6. 为什么不直接让 AI 自动改预算/出价？(受控 Agent 的边界设计)

**一句话**：不是技术做不到，而是真实投放场景里 AI 自动改配置的风险高于收益——真实业务团队对"动态配置"都很谨慎（生效延迟 + 持续维保成本），所以产品定位为"诊断辅助 + 人工审批"。

**展开要点**：
1. 真实业务佐证：投放系统动态配置有生效延迟，且媒体接口变更频繁，一旦 AI 自动改配置需持续投入维保，团队倾向于优先落地低维护成本的能力。
2. 风险维度：投放是高资金风险场景，自动执行出错的代价远高于"人工确认"的成本。
3. 产品价值：受控 Agent 在提升诊断效率的同时保留人工风控边界——这是比"全自动 Agent"更符合行业现实的产品判断。
4. 面试话术：**"我把 AI 定位成决策辅助而非决策执行，因为高风险场景下，可解释、可审核、可回滚比自动化更重要。"**

**证据点**：产品定义 §1（产品定位）、§12（执行边界）；真实业务沟通纪要（动态配置约束，脱敏引用）。

---

### Q7. 分层可解释诊断（管理摘要/运营解释/技术证据）是怎么设计的？

**一句话**：三层结构共用同一份结构化诊断事实（causeTree + timeline），前端逐层披露、不预设角色——管理者看摘要、运营看原因、技术看证据，同一份事实三种解读，不是三套割裂结论。

**展开要点**：
1. 三层：管理摘要（问题是什么/影响多大/要不要处理，30 秒）→ 运营解释（指标变化/原因排序/操作建议/观察指标）→ 技术证据（RTAID/实验 ID/分桶/日志字段/配置前后值）。
2. 关键设计：三层引用同一份 causeTree 和 timeline，保证原因、指标、时间口径一致。
3. 不预设角色：前端默认展示管理摘要，通过"查看原因""查看技术证据"逐层披露，用户按需展开。
4. 配套术语翻译表：参竞率→平台同意参与竞价的请求占比、RTA 超时→媒体没在规定时间内收到决策、分桶→按固定规则把流量分到不同实验组。

**证据点**：产品定义 §6；demo6 抽屉七段式 + 术语对照表。

---

### Q8. 10 状态机的设计思路？为什么需要一个完整的状态流转？

**一句话**：诊断不是一次性的"出结论"，而是一条从发起到复盘的完整业务闭环——读取 → 诊断 → 人工确认 → 人工执行 → 数据回流 → 复盘，每一步都对应真实业务中的一个动作和责任主体。

**展开要点**：
1. 状态体现 human-in-the-loop：待人工确认、等待人工执行、等待数据回流 三个状态是"人"的环节，说明 AI 只是辅助。
2. 异常分支完整：数据不足（不编造）、诊断失败（可重新诊断）、任意环节可关闭抽屉（不阻塞用户）。
3. 每个状态有明确责任主体：读取/诊断是 AI，确认/执行/复盘是人——面试时可指"状态机的'待人工确认'这个节点，就是产品设计里 human-in-the-loop 的落点"。

**证据点**：PRD §6 状态机；demo6 状态流转。

---

## 四、架构与技术

### Q9. 规则引擎 D1-D14 是怎么组织的？

**一句话**：按诊断链路从上游到下游组织，分三档优先级——P0 配置层硬检查（D1-D5，短路）、P1 链路指标层（D6-D10，阈值判定）、P2 边界/效果层（D11-D14，出价/回传/预算/CPA）；每个规则独立函数、可单独替换为真实 API。

**展开要点**：
1. P0（D1-D5）：RTAID 可用/账户绑定/QPS 容量/策略发布/实验运行——配置层，判定快、误报少，任一失败即短路。
2. P1（D6-D10）：分桶/请求量/命中率/参竞率/执行质量——链路指标，依赖报表数据。
3. P2（D11-D14）：出价配置/回传归因/预算达成率/CPA 判定——效果与可信度层。
4. 设计要点：每个 checkD* 独立，输入 ConfigSnapshot + MetricBundle，输出统一 signal 结构（ruleId/category/name/confidence/impact/evidence），后续接真实 API 时逐个替换即可。

**证据点**：MVP 诊断规则表；demo6 RULES 数组。

---

### Q10. 场景识别（S0-S7）是怎么做到"一个引擎诊断多种问题"的？

**一句话**：6 个典型问题不是 6 个独立引擎，而是"卡点定位 × 现象标注"两个维度的组合——卡点定位按诊断链路短路判断（S0 配置故障→S7 无异常），现象标注叠加业务标签（预算未达标/CPA 过高/可安全放量），两者组合映射到 6 个典型问题。

**展开要点**：
1. 卡点维度：S0-S7 按链路短路，第一个卡点就是根因场景（请求量→命中率→参竞率→出价→执行→回传）。
2. 现象维度：业务现象叠加在卡点上（如"跑不出去 + 根因在参竞率"= 典型问题①）。
3. 扩展性：加新场景不改规则引擎，只加"场景识别分支 + 叙事模板"——这就是"规则引擎是全链路体检，场景只是体检报告的不同解读方式"。
4. 面试话术：**"当前 MVP 聚焦一个场景，但底层是 D1-D14 全链路规则引擎；扩展新场景不是重写引擎，而是加'场景识别 + 场景叙事'两层。"**

**证据点**：场景扩展方案 §1-2；demo6 detectScenario。

---

### Q11. AI 层怎么设计成"后续可接入真实 LLM"的？

**一句话**：AI 层是独立模块（runAiLayer），输入输出有固定接口契约（AILayerInput → AILayerOutput），Mock 阶段用确定性模板占位，接入真实 LLM 时只需替换函数体，前端零改动。

**展开要点**：
1. 接口契约：输入 = causeTree + timeline + configChanges + budget + userContext（对齐契约 §3）；输出 = oneLiner/managerSummary/operationsNote/causes/affectedScope/recommendations（对齐 §4）。
2. 纯函数设计：无副作用、不查库、不调 API（LLM 调用只发生在函数体内）。
3. 可替换性：Mock 模板和真实 LLM 共用同一接口形状和同一评测标准，接入后行为可对比。
4. 评测兜底：接入真实 LLM 后用 Golden Case 同一把尺子评测，模板版若高分说明尺子太松，会同步收紧。

**证据点**：AI 层设计与评测方案 §4.1（接口形状）+ §5.1（评测对象）；demo6 runAiLayer。

---

## 五、数据与可信度

### Q12. 数据从哪来？是真实数据吗？

**一句话**：全部是 Mock 数据（模拟数据），标注"模拟"；但字段口径、页面结构、阈值来自真实 PRD 和 22 张真实平台截图，不是凭空想象的——不确定的媒体侧字段保留原名并标注【待核验】。

**展开要点**：
1. 来源：真实平台 PRD（R1/R2）、22 张截图（只作内部参考）、真实监控阈值（如 QPS 使用率>80%、超时率>3%、参竞率<80%）。
2. 处理：截图中的真实客户名/账户 ID/金额全部脱敏，替换为 mock（如"XX 电商代理"、夏季大促项目）。
3. Golden Case 是构造的模拟案例：预算 1000/消耗 300/门槛 40%→80%，用于验证诊断链路和评测 AI。
4. 待核验项：巨量开放平台字段表（Kiki 待补），真实接入前必须核验，不阻塞 mock MVP。

**证据点**：产品定义 §9 数据策略；对象模型 §5 待核验清单。

---

### Q13. 有没有评测机制证明 AI 是对的？

**一句话**：有——Golden Case 标准答案评测，分四层：L1 判定正确性（D1-D14 必须 100% 一致）、L2 归因正确性（主因命中 + 因果链方向，一票否决）、L3 解释质量、L4 规划质量（打分制）。

**展开要点**：
1. L1：规则引擎判定 vs 标准判定，必须全对（判定错则归因必错）。
2. L2（一票否决）：主因=门槛变更、因果链方向=门槛→参竞→曝光→消耗、单主因时次因=无——这三条错任何一条整体不合格。
3. L3/L4：管理摘要三要素、建议动作命中、方案三要素齐全，打分制。
4. 防止自欺：Mock 模板若拿高分说明尺子太松，真实 LLM 接入后同一把尺子重测。

**证据点**：AI 层设计与评测方案 §5；可做成评测脚本现场跑。

---

## 六、竞品与行业

### Q14. 市面上有类似产品吗？你和它们有什么区别？

**一句话**：投放平台本身有数据看板和规则告警（告诉我哪个指标超了），但没有"把多源信号合成因果链、解释原因、生成实验方案"的诊断 Agent；我这个是规则告警之上的受控 AI 诊断层。

**展开要点**：
1. 现状：真实平台有 RTA 报表、监控告警（QPS 超 80% 告警、命中率低于 50% 告警）——它们回答"哪里红了"。
2. 我的差异：回答"为什么红、影响多大、怎么验证"——规则告警给事实，我给的是一整条可解释的判断链和可执行的验证方案。
3. 与通用 AI 助手差异：不是通用聊天，是嵌入平台、有领域规则、有人工审批边界的产品化 Agent。
4. 与自动化投放 Agent 差异：不自动执行，强调可解释、可审核、可回滚。

**证据点**：框架定义 §1（定位）；R1 监控规则（真实告警阈值）。

---

### Q15. 你怎么看 AI Agent 在广告投放领域的边界？

**一句话**：AI 在投放领域应该先做"诊断和决策辅助"（理解数据、解释原因、设计方案），再做"自动执行"——因为投放是高资金风险场景，自动执行的前提是可解释、可评测、可回滚，这三件事行业还在探索，所以受控 Agent 是当前更合理的产品形态。

**展开要点**：
1. 短期可行：诊断/归因/方案生成——数据理解类能力，风险可控，价值明确。
2. 长期探索：自动执行/自动调价——依赖归因可信度、评测机制、风控兜底成熟。
3. 行业趋势佐证：真实业务里"模型建设是数据合作，不属于平台原生能力"，智能决策层本身就倾向外接/合作而非平台全包。
4. 我的项目正是踩在"先做诊断辅助"这个位置上。

**证据点**：真实业务纪要（模型外接，脱敏）；产品定义 §1。

---

## 七、扩展与规划

### Q16. 项目后续怎么扩展？

**一句话**：三条线——场景线（把 6 个典型问题逐个补全叙事）、能力线（AI 层接入真实 LLM + 评测集扩展）、工程线（离线化可运行 + 真实字段核验）。

**展开要点**：
1. 场景线：V0.2.5 CPA 标签补全 → V0.4 逐场景叙事（S0-S2、S4-S6）→ 6 典型问题全覆盖，复用同一引擎。
2. 能力线：AI 层接真实 LLM；评测集扩展（多主因/排除项/回传异常）。
3. 工程线：Kiki 字段表核验 → 真实字段映射 → 规则引擎接真实 API。
4. 架构保障：加场景不加引擎（场景识别 + 叙事两层），所以扩展成本可控。

**证据点**：PRD §13 后续规划；场景扩展方案 §7。

---

## 八、软技能/复盘

### Q17. 项目过程中你遇到的最大困难是什么？怎么解决的？

**一句话**：最大的困难是把"AI 价值"从玄学变成可验证的东西——一开始 Demo 的归因/解释/规划是模板拼接假扮的，我通过"替代测试"（能不能 if-else 写尽）把 AI 的不可替代部分收敛为归因/解释/规划三件事，并设计了 Golden Case 评测来证明它做对了。

**展开要点**：
1. 问题：demo 里 AI 层是空的（字符串拼接模板），面试官会问"这用规则引擎就够了，为什么要 AI"。
2. 方法：替代测试逐条筛选输出，筛出 if-else 写不尽的（归因/解释/规划）；设计 Golden Case 标准答案 + L1-L4 评测。
3. 结果：AI 价值从"我认为有用"变成"评测可验证"。
4. 第二个困难（可选）：Demo 本地双击白屏（ESM + CDN + file:// 限制）——通过 UMD 化 + 内联依赖 + 错误捕获解决，沉淀为"作品集 demo 应优先零外网依赖 + 普通 script"的经验。

**证据点**：AI 层设计与评测方案 §1/§5；demo 迭代记录。

---

### Q18. 如果重做一次，你会改什么？

**一句话**：会把 AI 层接入真实 LLM 和评测脚本前置到更早阶段——先验证"AI 价值可实证"，再做视觉和交互打磨。

**展开要点**：
1. 优先级调整：先做"核心价值验证"（AI 真实接入 + 评测闭环），再做"表面完整"（视觉/交互/多场景）。
2. 数据先行：先确认真实字段口径（Kiki 核验），再构造 mock，避免 mock 字段与真实字段脱节。
3. 少走弯路：Demo 技术选型一开始就选"零外网依赖 + 普通 script"，避免 ESM/CDN 白屏问题反复。

**证据点**：项目迭代回顾。

---

## 九、项目三一句话总结（面试结尾用）

> **"我设计了一个面向 RTA 投放优化的受控 Agentic Workflow：规则引擎做全链路判定，场景识别定位异常类别，AI 负责归因、解释和实验方案生成，并用 Golden Case 评测证明 AI 价值——在提升诊断效率的同时，通过人工审批保留了广告投放的风控边界。"**
