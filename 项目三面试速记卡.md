# 项目三 · 面试速记卡（一页版）

> 面试前 10 分钟扫一遍。每个问题只有一句话回答，被追问再用《项目三面试问答库》展开。

---

## 项目一句话

> 面向 RTA 投放优化的受控 Agentic Workflow：规则引擎全链路判定 → 场景识别定位异常类别 → AI 负责归因/解释/方案生成，用 Golden Case 评测证明 AI 价值，人工审批保留风控边界。

---

## 必考四连（背熟）

**Q：为什么用 AI 而不是纯规则？**
> 规则引擎回答"哪个环节出问题"（判定，if-else 能写尽）；AI 回答"为什么、影响多大、怎么验证"（归因/解释/规划，开放空间，if-else 写不尽）。

**Q：怎么保证 AI 不乱说？**
> 四道防线：① AI 只消费规则引擎结构化事实，不算数不查数；② 数据不足强制输出"数据不足"，不编造；③ 方案必须人工审批；④ Golden Case 标准答案评测（L2 主因一票否决）。

**Q：为什么不自动改预算/出价？**
> 不是做不到，是高资金风险场景里可解释、可审核、可回滚比自动化更重要；真实业务对动态配置都谨慎（延迟+维保成本）。

**Q：怎么证明 AI 做对了？**
> Golden Case 标准答案评测：L1 判定 100% 一致 → L2 归因（主因+因果链，一票否决）→ L3/L4 解释与方案打分。

---

## 其他高频（一句话）

**为什么选"跑不出去"做 MVP？** 贯穿 RTA 全链路、资料依赖度最高、诊断链路最清晰、AI 价值最好解释。

**一个引擎怎么诊断多种问题？** 6 个典型问题 = 卡点(S0-S7) × 现象(3标签) 组合，加场景不改引擎。

**分层可解释怎么设计？** 三层共用同一份 causeTree：管理摘要(30秒) → 运营解释(原因/建议) → 技术证据(可追溯)，逐层披露不预设角色。

**数据哪来的？** 全 Mock 但字段/阈值/页面来自真实 PRD + 22 张截图（脱敏），项目层是作品集扩展，媒体字段标【待核验】。

**10 状态机为什么这么设计？** 诊断是完整业务闭环：读取→诊断→人工确认→人工执行→回流→复盘，"待人工确认"就是 human-in-the-loop 的落点。

**AI 层怎么接真实 LLM？** 独立模块 + 固定接口契约（输入 causeTree+scenario → 输出文案字段），Mock 模板占位，替换函数体即可，前端零改动。

**后续怎么扩展？** 场景线（6 问题逐个补叙事）、能力线（接真实 LLM + 评测集扩展）、工程线（字段核验 + 接真实 API）。

**最大困难？** 把"AI 价值"从玄学变实证——用替代测试筛出 AI 不可替代部分（归因/解释/规划），再设计 Golden Case 评测证明。

**重做会改什么？** AI 价值验证（真实 LLM + 评测）前置到最早期；数据口径先行；技术选型一开始就选零外网 + 普通 script。

---

## 加分金句（可选讲）

- "决策快 ≠ 排查快"：RTA 单次决策 100ms，但"今天预算为什么没跑出去"是天级归因，系统越自动排查越难。
- "规则引擎是全链路体检，场景是体检报告的不同解读方式。"
- "模型建设在真实业务里属于数据合作，不是平台原生能力"——智能决策层本身就倾向外接，我的受控 Agent 符合行业趋势。
