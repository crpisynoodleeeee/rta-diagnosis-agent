# 项目三：MVP 诊断规则表 v0.1

> MVP 主场景：**有预算但跑不出去**。
> 本表将《项目三框架定义》第 6 节的 12 步诊断链路，转化为「检查节点 → 异常信号 → 可能原因 → 验证动作 → 排除条件 → 实验建议」的结构化规则，是 Agent 诊断引擎（规则层）与 AI 原因排序（模型层）共同消费的事实基础。
> 指标口径与告警阈值来源：R1 监控规则、R2 业务规则；未确认的阈值标注【待核验】。

---

## 1. 规则表总览

| 节点 | 检查内容 | 优先级 | 依赖数据（对象模型） | 典型异常 |
| --- | --- | --- | --- | --- |
| D1 | RTAID 是否可用 | P0 | O3 RTAID | 下线 / 未配置 BidURL / SecretKey 缺失 |
| D2 | 媒体账户是否正确绑定 | P0 | O2/O3 | 未绑定 / 绑定被移除 |
| D3 | QPS 是否配置不足或占满 | P0 | O3/O4 | 使用率 > 80% / 超限流 |
| D4 | 策略是否已发布并绑定 RTAID | P0 | O5 | 草稿 / 已下线 / 已停用 / 未绑定 |
| D5 | 实验是否处于生效状态 | P0 | O7 | 草稿 / 已暂停 / 已结束 / 未发布 |
| D6 | 分桶是否命中 / 大量兜底 | P1 | O8 | 大量进入对照组 / 桶 ID 不匹配 |
| D7 | 请求量是否正常 | P1 | O11 | 请求量骤降 / 为空 |
| D8 | 命中率是否异常偏低 | P1 | O11 | 命中率 < 50% |
| D9 | 放行率（参竞率）是否异常偏低 | P1 | O11 | 参竞率 < 80% 或大幅偏离基线 |
| D10 | 执行失败率 / 超时率是否异常 | P1 | O10/O11 | 失败率 > 5% / 超时率 > 3% / P99 > 100ms |
| D11 | 出价配置是否过严 | P2 | O5 | 出价系数过低 / 门槛过高 / 出价低于建议范围 |
| D12 | 转化回传 / 归因是否影响效果判断 | P2 | 回传 API 资料 | 回传成功率下降 / 归因口径变化 |

> 优先级说明：P0 = 配置层硬检查（数据源为对象配置，判定快、成本低、误报少），P1 = 链路指标层（依赖报表数据，需要时间窗口），P2 = 效果可信度层（用于解释"为什么现象看起来异常"，不阻断 P0/P1 结论）。

---

## 2. 详细规则

### D1 RTAID 是否可用

| 项 | 内容 |
| --- | --- |
| 检查项 | RTAID 状态、BidURL、SecretKey（华为）、token（小米） |
| 异常信号 | RTAID 状态=下线；BidURL 为空或格式非法；华为无 SecretKey；小米无 token |
| 判定阈值 | 状态枚举：上线 / 下线（硬判定） |
| 可能原因 | RTAID 被误下线；媒体侧资源失效；配置未完成上线校验 |
| 验证动作 | 查看 RTAID 配置详情与操作日志；确认最近下线操作人/时间 |
| 排除条件 | 状态=上线 且 BidURL/密钥完整 → 排除 |
| 证据输出 | `RTAID 状态 / BidURL / 签名配置 / 最近操作` |
| 实验建议 | 该节点异常时不进入实验建议，先提示修复配置 |

### D2 媒体账户是否正确绑定

| 项 | 内容 |
| --- | --- |
| 检查项 | RTAID 绑定账户数、绑定对象（小米为创意/token） |
| 异常信号 | 绑定账户数 = 0；绑定对象失效（小米创意未绑 token 的创意不触发 RTA 请求） |
| 判定阈值 | 绑定账户数 ≥ 1（硬判定）；小米：token 与创意绑定完整性【待核验】 |
| 可能原因 | 账户解绑；新账户未绑定；小米创意未绑定 token |
| 验证动作 | 核对 RTAID 配置的绑定列表；小米场景核对创意绑定关系 |
| 排除条件 | 绑定 ≥ 1 且对象有效 → 排除 |
| 证据输出 | `绑定账户数 / 绑定对象 / 小米 token 绑定状态` |
| 实验建议 | 同 D1，先修复配置 |

### D3 QPS 是否配置不足或已被占满

| 项 | 内容 |
| --- | --- |
| 检查项 | 配置 QPS、实验已占用 QPS、剩余 QPS、当前 QPS 使用率 |
| 异常信号 | QPS 使用率 > 80%（黄色）；> 100%（红色高危）；触发 RTA 限流（执行日志出现「系统异常-限流」）；剩余 QPS < 0 |
| 判定阈值 | 使用率 = 当前 QPS / 配置 QPS；告警阈值 80% / 100%（R1）；剩余 QPS = 配置 − 实验占用（R2） |
| 可能原因 | 实验占满配额；请求量突增；配置 QPS 过低；系统 QPS 上限被占满 |
| 验证动作 | 看 RTA 报表 QPS 趋势与限流记录；核对系统 QPS 上限（默认 300000）与已分配 |
| 排除条件 | 使用率 < 80% 且无限流记录 → 排除 |
| 证据输出 | `配置 QPS / 已占用 / 剩余 / 使用率 / 限流次数` |
| 实验建议 | 提升 RTAID QPS 配额或减少并行实验占用（实验建议中给出 QPS 释放方案） |

### D4 策略是否已发布并绑定当前 RTAID

| 项 | 内容 |
| --- | --- |
| 检查项 | 实验引用的策略状态、策略绑定的 RTAID 是否=当前 RTAID、策略数据源是否可用 |
| 异常信号 | 策略状态 ∈ {草稿, 已下线, 已停用}；策略绑定其他 RTAID；数据源不可用（覆盖人数「暂无法计算」） |
| 判定阈值 | 状态枚举硬判定；数据源可用性为软判定 |
| 可能原因 | 策略发布后未同步；策略被下线/停用；RTAID 变更后策略未重新绑定（编辑态不允许改 RTAID，需复制重建） |
| 验证动作 | 策略工厂核对策略状态与 RTAID；核对数据源与覆盖人数计算状态 |
| 排除条件 | 策略=已发布 且 绑定当前 RTAID 且数据源可用 → 排除 |
| 证据输出 | `策略状态 / 绑定 RTAID / 数据源状态 / 覆盖人数` |
| 实验建议 | 该节点异常时优先修复策略状态，不进入策略对比实验 |

### D5 实验是否处于生效状态

| 项 | 内容 |
| --- | --- |
| 检查项 | 实验状态、生效机制（立即/定时）、生效时段、实验结束时间 |
| 异常信号 | 实验状态 ∈ {草稿, 已暂停, 已结束}；定时生效未到生效时间；已过结束时间；生效时段不覆盖当前时间 |
| 判定阈值 | 状态枚举硬判定；生效时段区间判定 |
| 可能原因 | 实验暂停/结束未恢复；定时发布未生效；生效时段配置错误 |
| 验证动作 | 实验详情核对状态与发布时间/生效时段 |
| 排除条件 | 状态=实验中 且 当前时间在生效时段内 → 排除 |
| 证据输出 | `实验状态 / 生效机制 / 生效时段 / 剩余生效时间` |
| 实验建议 | 恢复实验或调整生效时段（如为定时实验） |

### D6 分桶是否命中或大量进入对照组兜底

| 项 | 内容 |
| --- | --- |
| 检查项 | 分桶方式（平台/客户）、对照组 vs 实验组流量占比、兜底执行数（分桶缺失/不匹配/计算失败） |
| 异常信号 | 实验组请求占比远低于分桶配置占比；对照组兜底占比异常升高（媒体未传桶 ID / 桶 ID 不匹配 / 客户分桶字段缺失）；兜底执行数占比升高 |
| 判定阈值 | 实验组实际流量占比 vs 配置占比偏差 > 30%【待核验】；兜底执行占比环比升高（R1 监控项） |
| 可能原因 | 媒体未传桶 ID（平台分桶依赖媒体传入）；平台分桶 ID 配置不匹配；客户分桶字段缺失或全空（走空设备 ID 兜底）；分桶计算失败 |
| 验证动作 | 对比分桶配置与实际流量分布；查执行日志分桶字段；核对客户分桶字段回传情况 |
| 排除条件 | 流量占比与配置一致 且 兜底占比正常 → 排除 |
| 证据输出 | `分桶方式 / 各组分桶占比（配置 vs 实际）/ 兜底执行数及原因` |
| 实验建议 | 修复分桶 ID 映射或客户分桶字段；必要时重建实验分桶 |

### D7 请求量是否正常

| 项 | 内容 |
| --- | --- |
| 检查项 | 请求总量、请求趋势、环比/同比 |
| 异常信号 | 请求总量骤降（如环比下降 > 50%【待核验】）；请求量持续为 0；请求集中在少数时段 |
| 判定阈值 | 请求量环比变化率【待核验】：|Δ| > 30% 关注，> 50% 异常 |
| 可能原因 | 媒体侧流量波动（大盘/季节）；创意下架或素材问题；投放计划暂停；RTAID 在媒体侧被限流 |
| 验证动作 | 对比媒体大盘趋势；核对创意与计划状态；查执行日志请求来源分布 |
| 排除条件 | 请求量平稳或上升 → 排除 |
| 证据输出 | `请求总量 / 趋势 / 环比 / 时段分布` |
| 实验建议 | 该节点异常说明问题在入口流量，不调整 RTA 策略；建议先排查媒体侧 |

### D8 命中率是否异常偏低

| 项 | 内容 |
| --- | --- |
| 检查项 | 策略命中率（命中数 / 请求总量）、命中趋势 |
| 异常信号 | 命中率 < 50%（R1 告警阈值）；命中率相对历史基线大幅下降（如 −20pp【待核验】） |
| 判定阈值 | < 50% 告警（R1）；相对基线偏离【待核验】 |
| 可能原因 | 人群包过期/覆盖下降；人群包同步失败；用户字段匹配失败（字段 ID 变更/数据源不可用）；模型阈值变严；新规则误伤 |
| 验证动作 | 核对人群覆盖预估变化；查执行日志字段匹配结果；核对数据源与字段配置变更记录 |
| 排除条件 | 命中率 ≥ 50% 且稳定 → 排除 |
| 证据输出 | `命中率 / 命中数 / 覆盖人数变化 / 字段匹配失败样例` |
| 实验建议 | 扩大候选人群 / 放宽用户字段 / 回滚规则，用 AB 实验验证恢复效果 |

### D9 放行率（参竞率）是否异常偏低

| 项 | 内容 |
| --- | --- |
| 检查项 | 参竞率（参竞数 / 请求总量）、拒绝率、放行率（命中后放行比例） |
| 异常信号 | 参竞率 < 80%（R1 偏离告警）；放行率相对基线大幅下降；拒绝率异常升高 |
| 判定阈值 | < 80% 告警（R1）；放行率相对基线偏离 > 20pp【待核验】 |
| 可能原因 | 策略条件过严（门槛/阈值上调）；反向策略误配；出价规则导致放行减少；模型打分分布偏移 |
| 验证动作 | 对比策略配置变更记录（门槛/阈值前后值）；查执行日志策略执行结果分布；核对参竞策略正反向配置 |
| 排除条件 | 参竞率 ≥ 80% 且稳定 → 排除 |
| 证据输出 | `参竞率 / 放行率 / 拒绝率 / 策略配置变更前后值` |
| 实验建议 | **核心场景**：门槛回调小流量试跑（对应 goldenReport 方案），观察参竞率/曝光/消耗/CPA |

### D10 执行失败率 / 超时率是否异常

| 项 | 内容 |
| --- | --- |
| 检查项 | 执行失败率、失败原因分布、超时率、P95/P99 耗时、兜底执行数 |
| 异常信号 | 失败率 > 5%【待核验，R1 以成功率 < 95% 告警】；超时率 > 3%（R1）；P99 > 100ms（R1）；平均耗时 > 50ms（R1）；兜底占比升高 |
| 判定阈值 | 成功率 < 95% / 超时率 > 3% / P99 > 100ms（R1 监控规则） |
| 可能原因 | 系统限流（QPS 超限）；RTA 传入参数异常；媒体侧请求异常；模型异常；BidURL 服务故障 |
| 验证动作 | 按失败原因枚举（R1）分桶统计；查执行日志响应耗时与失败原因；核对 BidURL 服务可用性 |
| 排除条件 | 成功率 ≥ 95% 且耗时/超时正常 → 排除 |
| 证据输出 | `成功率 / 失败率 / 失败原因分布 / P95 / P99 / 超时率` |
| 实验建议 | 该节点异常优先排障（容量/服务/参数），不调整策略实验 |

### D11 出价配置是否过严

| 项 | 内容 |
| --- | --- |
| 检查项 | 出价方式（系数/CPA/CPC）、出价配置模式（固定/模型）、出价值、出价系数范围、参竞比例 |
| 异常信号 | 出价系数过低（如 < 0.2，腾讯合法范围下限）；CPA/CPC 低于建议范围 100（R1 检查项）；参竞比例配置过低（全量参竞 1%–100% 中取值偏低）；模型出价区间过窄导致命中不足 |
| 判定阈值 | 腾讯出价系数 0.2–5.0；CPA/CPC 建议 100–10000（R1 发布检查项，可复用为诊断阈值） |
| 可能原因 | 出价竞争力不足；模型出价区间过严；参竞比例配置保守 |
| 验证动作 | 对比竞品/行业出价区间【待核验】；核对模型分区间命中分布；查执行日志出价执行信息 |
| 排除条件 | 出价在合理区间且竞胜率正常 → 排除 |
| 证据输出 | `出价方式 / 出价值 / 模型分区间 / 平均出价 / 竞胜率` |
| 实验建议 | 提价或放宽出价区间的小流量实验，明确提价上限与止损条件 |

### D12 转化回传 / 归因是否影响效果判断

| 项 | 内容 |
| --- | --- |
| 检查项 | 回传成功率、回传延迟、归因口径变化、回传识别字段完整性（click_id/callback） |
| 异常信号 | 回传成功率下降；回传延迟升高；归因配置调整记录；转化事件与媒体目标不匹配 |
| 判定阈值 | 回传成功率基线【待核验】：下降 > 5pp 关注 |
| 可能原因 | 回传账号配置问题；回传条件过细导致部分转化未回传；事件映射错误；监测链接未关联 |
| 验证动作 | 核对回传日志失败原因（reason_2 辅助资料）；核对事件中心关联口径；对比归因前后 CPA 口径 |
| 排除条件 | 回传与归因口径稳定 → 排除 |
| 证据输出 | `回传成功率 / 回传延迟 / 归因口径变更 / 识别字段缺失率` |
| 实验建议 | 该节点作为"效果可信度检查"：结论附加可信度标注，不扩展为独立模块 |

---

## 3. 规则编排逻辑（决策顺序）

```text
启动诊断（用户选择 RTAID/项目 + 日期）
  │
  ├─ Phase 0 数据读取：拉取 O3/O4/O5/O7/O8 配置快照 + O10 日志摘要 + O11 报表指标
  │
  ├─ Phase 1 硬检查（P0 节点 D1→D5，按序执行，任一失败即短路并提示修复）
  │     D1 RTAID → D2 账户绑定 → D3 QPS → D4 策略 → D5 实验
  │
  ├─ Phase 2 链路指标（P1 节点 D6→D10）
  │     D6 分桶命中 → D7 请求量 → D8 命中率 → D9 放行率 → D10 执行质量
  │     （D6-D10 相互独立，全部执行后合并结果）
  │
  ├─ Phase 3 效果可信度（P2 节点 D11 出价 → D12 回传归因）
  │
  ├─ 输出：每条规则的结果 {节点, 判定(normal/abnormal), 置信度, 证据, 指标}
  │
  └─ AI 层：基于规则结果做原因排序（primary/secondary/excluded）+
           生成实验建议（action/before/after/observeMetrics/successCriteria/rollbackCondition）
```

规则引擎与 AI 分工：

| 层 | 职责 | 不做什么 |
| --- | --- | --- |
| 规则引擎 | 硬判定（配置枚举）、阈值判定、证据收集、短路 | 不做模糊归因、不写自然语言结论 |
| AI 层 | 综合多节点信号排序原因、判断因果链、生成实验方案 | 不计算指标、不伪造证据、不改变配置判定结果 |

---

## 4. 规则结果 → 诊断报告映射

| DiagnosisReport 字段 | 来源 |
| --- | --- |
| causeTree[]（节点级） | 每条规则输出：label=节点名、result=normal/abnormal、confidence、impact、evidence、metrics |
| causes.primary / secondary | AI 层对 abnormal 节点按 impact×confidence 排序，结合 D1-D12 因果链 |
| causes.excluded | AI 层对 normal 节点给出排除结论（含置信度） |
| timeline | D4/D6/D9 节点涉及配置变更时，由变更记录构造时间线；指标类节点（D8/D9/D10）贡献趋势事件 |
| impact | 预算/消耗/差额/达成率 + 受影响范围（来自 abnormal 节点的分桶/策略范围） |
| recommendations[] | AI 层基于 primary cause 生成，结构固定（action/before/after/impact/observeMetrics/successCriteria/rollbackCondition） |
| technical | 拼接 RTAID、实验 ID、组 ID、分桶、请求 ID、配置前后值、数据来源 |

---

## 5. 与 goldenReport 的对齐验证

goldenReport（demo `src/data/mock.ts`）对应规则执行结果：

| goldenReport 内容 | 命中规则 |
| --- | --- |
| 预算/消耗正常、状态投放中（causeTree c-budget normal） | 场景前置检查（预算层，非规则表 D 节点） |
| RTAID 上线、账户已绑定、QPS 42.8k/300k（c-binding normal） | D1 ✅ + D2 ✅ + D3 ✅（使用率 14% < 80%） |
| 请求量平稳、设备 ID 命中率 96%（c-traffic normal） | D7 ✅ + D8 ✅（96% ≥ 50%） |
| P95 18ms、超时率 0.4%、错误率 0.6%（c-service normal） | D10 ✅（成功率 99.4% ≥ 95%，P95 18ms < 阈值） |
| 实验中、分桶 10/10 生效（c-experiment normal） | D5 ✅ + D6 部分 ✅（分桶生效但策略层异常） |
| **实验组参竞率 60% → 10%（c-strategy abnormal，高影响）** | **D9 ✅ abnormal**（参竞率 < 80%，放行率骤降 −50pp） |
| 曝光 −80%（c-bidding abnormal，中影响） | D11 关联 ✅（出价未调但曝光随参竞率下滑） |
| 回传成功率 99.2%（c-attribution normal） | D12 ✅（可信度正常，不影响结论） |
| 结论：实验组门槛 40%→80% 导致参竞率骤降 | D9 primary（由配置变更 D4 时间线佐证） |
| 建议：门槛 80% → 60% 小流量试跑 | D9 实验建议（回调 + 观察参竞率/曝光/消耗/CPA） |

> 验证结论：goldenReport 与规则表完全对齐，说明规则表可以直接驱动 demo 的 mock 输出；后续接入真实 Agent 时，规则引擎产出的 causeTree 结构可无缝替换 mock 数据。

---

## 6. 待核验阈值清单

1. 请求量环比异常阈值（30% / 50%）——作品集假设值，需真实数据校准。
2. 放行率相对基线偏离阈值（20pp）——同上。
3. 实验组实际流量占比偏差阈值（30%）。
4. 回传成功率下降关注阈值（5pp）。
5. 失败率阈值（5%，R1 以成功率 < 95% 表达）。
