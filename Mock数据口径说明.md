# Mock 数据口径说明

> 用途：说明 Demo 中数据的来源层级、合成规则和真实接入前的核验边界。
> 关联：《RTA 对象模型与字段清单》《MVP 诊断规则表》《场景扩展方案》

---

## 1. 数据来源层级总览

| 层级 | 含义 | 处理方式 |
| --- | --- | --- |
| **真实口径** | 来自真实平台 PRD/截图/监控规则，字段名与阈值可信 | 保留原名，可直接引用 |
| **Demo 扩展字段** | 为覆盖预算未达标等诊断现象而抽象的对象（如项目层预算/消耗/达成率）| 标注为 Demo 扩展 |
| **待核验** | 媒体侧字段，真实接入前需核验 | 保留原名 + 【待核验】标注 |

---

## 2. 真实口径字段（可直接引用）

| 字段/指标 | 来源 | 说明 |
| --- | --- | --- |
| RTAID / BidURL / SecretKey / token | R1/R2 PRD | 媒体侧资源标识与签名配置 |
| 配置 QPS / 实验已占用 QPS / 剩余 QPS | R2 §9.5 | 剩余 = 配置 − 已占用 |
| 系统 QPS 上限（默认 300000）| R2 §10.5 | 租户/系统全局额度 |
| 策略状态（草稿/已发布/已下线/已停用）| R2 §8.2 | 发布校验规则 |
| 实验状态（草稿/实验中/已暂停/已结束）| R2 §11.1 | 生效机制/时段 |
| 分桶（10 桶，每组至少 1 桶）| R2 §11.5 | 平台/客户分桶兜底规则 |
| 当前 QPS / 峰值 QPS / QPS 使用率 | R1 监控指标 | 使用率 >80% 黄 / >100% 红 |
| 平均耗时 / P95 / P99 | R1 监控指标 | >50ms / P99 >100ms 告警 |
| 成功率 / 失败率 / 超时率 | R1 监控指标 | 成功率 <95% / 超时率 >3% 告警 |
| 请求量 / 命中率 / 参竞率 / 拒绝率 | R1 使用情况指标 | 命中率 <50% / 参竞率 <80% 告警 |
| 兜底执行占比 | R1 监控项 | 异常升高提示 |
| 出价系数范围 0.2-5.0 | R1 发布检查 | 腾讯合法范围 |
| CPA/CPC 建议范围 100-10000 | R1 发布检查 | 低于/高于告警 |
| 执行失败原因枚举 | R1 | 限流/参数异常/模型异常/服务故障/未知 |

## 3. Demo 扩展字段（标注为扩展）

| 字段 | 说明 |
| --- | --- |
| 项目（Project）| 投放管理层扩展对象，真实 RTA 域无此层，用于表达预算未达标等业务现象 |
| 日预算 / 实际消耗 / 预算达成率 | 项目层业务指标，构成触发条件（达成率 = 消耗/预算）|
| Golden Case 数值 | 预算 1000 / 消耗 300 / 门槛 40%→80% 为构造的模拟案例，用于验证链路与评测 |
| conversionCount / actualCpa / targetCpa | Demo 补充字段（CPA 现象标签用），口径为通用定义 |
| QueryContext / evidenceId | v0.7 查询层扩展对象，用于把当前 RTA 的指标、趋势、组间、配置变更和诊断证据统一编号 |

## 3.1 v0.7 QueryContext 口径

`QueryContext` 由 `performDiagnosis(record)` 基于同一条 Mock RTA 记录和 `DiagnosisReport` 程序化生成，不引入新数据源。

| 类型 | evidenceId 示例 | 来源 |
| --- | --- | --- |
| 指标 | `EV-METRIC-ACTUALCPA` / `EV-METRIC-DAILYBUDGET` | Mock 记录中的 `coreMetrics` / `usageMetrics` / `budget` / CPA 字段 |
| 趋势 | `EV-TREND-001` | Mock 记录中的 `trend[]` |
| 配置变更 | `EV-CHANGE-001` | Mock 记录中的 `changes[]` |
| 实验组/对照组 | `EV-GROUP-CONTROL` / `EV-GROUP-TREATMENT` | Mock 记录中的 `groups[]` |
| 规则证据 | `EV-RULE-D9` | 规则引擎 `runRules(record)` 输出 |

这些编号只用于 Demo 内证据回指，不是生产日志 ID。查询层只允许读取当前 RTA 的 Mock `QueryContext`，不做跨 RTA 历史、不查行业大盘、不访问真实媒体 API。

## 4. 待核验字段【待核验】

| 字段 | 待核验内容 | 阻塞性 |
| --- | --- | --- |
| 巨量 RTA 报表请求/返回字段 | 真实字段名与指标口径（Kiki 补截图）| 不阻塞 mock |
| 准入门槛（模型打分阈值）| 真实系统的字段名与口径 | 不阻塞 mock |
| 项目与 RTAID/策略/实验的绑定关系 | 真实绑定层级字段 | 不阻塞 mock |
| 预算/消耗/达成率数据来源表 | 真实数据来源 | 不阻塞 mock |
| 媒体侧出价字段换算 | VIVO 毫分→元、荣耀 priceRate 等 | 不阻塞 mock |

## 5. 公开数据声明

Demo 数据全部为合成 Mock 数据。字段名、页面结构和部分阈值依据公开可讨论的产品结构抽象；项目层预算、消耗和达成率是为覆盖预算诊断场景增加的 Demo 扩展对象。v0.7 QueryContext 只是在 Mock 数据上增加只读索引和证据编号，不代表真实报表或媒体接口联调。媒体侧不确定字段保留原名并标记【待核验】，真实接入前必须完成字段和指标口径核验。Golden Case 是构造案例，用于验证诊断链路和 AI 输出评测。
