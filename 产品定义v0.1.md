# 项目三：产品定义 v0.1

> 状态：v0.1（框架已确认，进入结构化产出阶段）
> 版本日期：2026-08-16
> 关联文档：《项目三框架定义》《项目三启动与输入清单》《项目三资料盘点与可用性分析》《RTA典型问题案例采集草稿》《RTA对象模型与字段清单 v0.1》《MVP诊断规则表 v0.1》《Agent输入输出结构契约 v0.1》《demo/README.md》

---

## 1. 产品定位（一句话）

> **RTA Strategy Diagnosis Agent：面向广告投放高风险场景的受控策略诊断与实验 Agent。**

它不是单纯的聊天式 Copilot，也不是完全自动执行的投放 Agent，而是一个嵌入投放平台的 **Human-in-the-loop Agentic Workflow**：

- 自动读取 RTAID、策略、实验、QPS、报表和执行日志等多源信号；
- 定位「有预算但跑不出去」的异常环节；
- 对可能原因排序并说明判断依据；
- 生成带观察指标、成功标准和回滚条件的实验方案；
- 保留人工确认与执行控制，AI 不直接修改真实投放配置。

## 2. 用户与使用场景

| 项 | 定义 |
| --- | --- |
| 首要用户 | 广告主侧负责 RTA 投放和策略运营的业务人员 |
| 日常动作 | 配置 RTA 策略、绑定媒体 RTAID、创建/维护 RTA 实验、观察报表和执行日志、排查跑量异常、调整策略/出价/分桶 |
| 核心痛点 | 遇到「跑不出去」时需在 RTAID 配置→账户绑定→策略工厂→RTA 实验→QPS→报表→执行日志→归因回传多个页面间人工排查，难以快速判断异常发生在哪一层、哪些证据支持、优先检查什么、下一步调整什么、如何低风险验证 |

## 3. 首版 MVP 场景

> **有预算但跑不出去。**（广告主已配置预算和投放目标，但实际请求、放行、竞价、曝光或消耗不达预期）

选择理由：

1. 与 RTAID、QPS、策略状态、实验状态、执行日志直接相关，RTA 资料依赖度最高；
2. 诊断路径清晰（12 步链路），适合做成可运行 Demo；
3. 面试时易解释 AI 价值：规则发现异常，AI 综合上下文解释原因并生成实验方案。

## 4. 产品形态

| 维度 | 定义 |
| --- | --- |
| 入口 | RTA 数据中心、RTA 实验详情、策略工厂详情、投放管理列表（Demo 从投放列表「发起诊断」进入） |
| 展示方式 | 右侧诊断抽屉（720px）/ 独立诊断工作台 |
| 数据来源 | 系统自动读取结构化数据（ConfigSnapshot + MetricBundle），用户补充业务上下文（UserContext） |
| 输出结果 | 异常定位、原因排序、证据说明、待检查项、实验建议 |
| 执行边界 | AI 生成实验草稿或调整建议，人工确认后执行；不自动修改真实预算、出价或策略 |

## 5. Agentic Workflow（7 阶段）

| 阶段 | Agent 能力 | 自动化程度 |
| --- | --- | --- |
| Observe | 读取 RTAID、策略、实验、QPS、报表、执行日志和回传风险 | 自动 |
| Diagnose | 判断异常所在环节（D1-D12 规则表），对可能原因排序 | 自动 |
| Explain | 按角色分层展示结论、影响、证据、缺失信息和判断依据 | 自动 |
| Plan | 生成实验方案、观察指标、成功标准和回滚条件 | 自动 |
| Draft | 生成策略或实验草稿 | 半自动 |
| Approve | 由运营人员确认是否采用 | 人工 |
| Learn | 复盘实验结果，沉淀案例 | 自动辅助 |

## 6. 分层可解释诊断（Explain 核心能力）

三层结构共用同一份结构化诊断事实（causeTree + timeline），前端逐层披露，不预设角色：

| 层级 | 面向 | 回答 | 默认展示 |
| --- | --- | --- | --- |
| 1. 管理摘要 | 老板/非专业同事 | 问题是什么、影响多大、是否需要处理 | ✅ 默认展示 |
| 2. 运营解释 | 投放运营 | 指标变化、原因排序、操作建议、观察指标和风险 | 「查看原因」展开 |
| 3. 技术证据 | 技术人员 | RTAID、实验 ID、分组、桶、日志字段、配置前后值、数据来源 | 「查看技术证据」展开 |

配套术语翻译表：参竞率（平台同意参与竞价的请求占比）、RTA 超时（媒体未在规定时间内收到决策结果）、分桶（按固定规则将流量分配到不同实验组）、准入门槛（只有模型打分高于此阈值的请求才允许参与竞价）等。

## 7. MVP 边界

### 7.1 纳入

- RTA 数据中心异常诊断（资源口径 + 分析口径指标）
- RTAID/QPS/媒体账户配置检查（D1-D3）
- 策略工厂配置检查（D4：状态、绑定、数据源）
- RTA 实验状态与分桶检查（D5-D6）
- 执行日志异常解释（D10）
- AI 原因排序（primary/secondary/excluded）
- AI 生成实验建议（含观察指标、成功标准、回滚条件）
- 人工确认后的实验草稿
- 归因与回传作为「效果可信度检查」（D12）

### 7.2 暂不纳入

- 建设真实 RTA 接口 / 对接真实媒体 API
- 自动修改真实预算、出价或策略
- 完整归因系统与回传 API 管理
- 多媒体全部能力矩阵（媒体差异仅在对象模型与规则表中保留边界说明）
- Apple Ads 独立归因场景

## 8. 诊断框架摘要

12 步诊断链路（详见《MVP诊断规则表 v0.1》）：

```text
D1 RTAID 可用 → D2 账户绑定 → D3 QPS 容量 → D4 策略状态 → D5 实验生效
→ D6 分桶命中 → D7 请求量 → D8 命中率 → D9 放行率/参竞率（核心）
→ D10 执行质量 → D11 出价配置 → D12 回传归因（可信度检查）
```

规则引擎与 AI 分工：

| 层 | 职责 | 不做什么 |
| --- | --- | --- |
| 规则引擎 | 硬判定、阈值判定、证据收集、短路 | 不模糊归因、不写自然语言 |
| AI 层 | 多信号综合排序、因果链、实验方案生成 | 不计算指标、不伪造证据、不改变判定结果 |

关键告警阈值（来源 R1 监控规则）：QPS 使用率 > 80% / 100%、平均耗时 > 50ms、P99 > 100ms、超时率 > 3%、成功率 < 95%、参竞率 < 80%、策略命中率 < 50%、兜底执行占比异常升高。

## 9. 数据策略

- 统一使用 **mock 数据**：模拟 RTAID、策略、实验、媒体账户、请求/命中/放行/QPS/执行状态、一条「有预算但跑不出去」的诊断案例（goldenReport）。
- 所有作品集展示数据标注为模拟数据，不直接使用原截图中的真实数据。
- 原系统截图（`R (2)/R/PICTURE`，22 张）仅作为内部设计参考，用于理解字段、页面结构和业务流程。
- 不确定的媒体侧字段保留原名称并标注【待核验】。

## 10. 验收标准（分角色）

| 角色 | 验收标准 |
| --- | --- |
| 管理者 | 30 秒内说清问题、影响和是否需要处理 |
| 运营人员 | 能复述主要原因，明确下一步操作及需观察的指标 |
| 技术人员 | 能从解释结果追溯到原始数据、配置和日志证据 |
| 目标用户（可用性） | 1 分钟内正确回答「发生了什么、为什么发生、接下来做什么」三个问题中的至少两个，不依赖额外讲解 |

## 11. 作品集表达重点

1. 这是广告投放高风险场景下的**受控 Agent**，而不是简单 Copilot；
2. AI 不替代数据计算和规则告警，负责**综合诊断、解释和实验方案生成**；
3. 产品设计采用 **human-in-the-loop**，保留人工审批和执行控制；
4. 方案基于真实平台资料抽象，但展示数据和页面重新设计（mock 标注）；
5. 首版 MVP 聚焦一个清晰问题：**有预算但跑不出去**。

推荐一句话表达：

> 本项目设计了一个面向 RTA 投放优化的受控 Agentic Workflow，自动读取策略、实验、QPS 和执行日志等多源信号，定位跑量异常原因并生成可审核的实验方案，在提升诊断效率的同时保留广告投放的人工风控边界。

## 12. 已确认决策汇总

| 决策项 | 结论 |
| --- | --- |
| MVP 主场景 | 有预算但跑不出去 |
| 首要用户 | 广告主侧 RTA 投放/策略运营业务人员 |
| 项目目的 | AI 求职作品，证明产品设计和 AI 应用能力 |
| 截图使用 | 仅内部设计参考，作品集重新设计 |
| 数据策略 | 全 mock，标注模拟数据 |
| 真实案例 | 无，项目构造模拟案例 |
| 最终形态 | PRD + 可运行 Demo 组合（Demo 已有 v1/v2） |
| 触发方式 | 用户选择计划后发起诊断（默认），系统自动读取数据 |
| 执行边界 | AI 生成草稿，人工确认执行 |
| 诊断范围 | 12 步链路，归因回传作为可信度检查 |

## 13. 下一步工作

1. **PRD v0.1**：基于产品定义 v0.1 + 对象模型 + 规则表，形成正式 PRD（页面、状态、权限、验收标准）。
2. **Demo 接入 Agent 骨架**：将规则表实现为本地规则引擎（TS），输出结构替换 mock goldenReport；后续可替换为 LLM 调用。
3. **Kiki 补充核验**：巨量开放平台字段表截图 → 核验项目预算、RTA 策略生效范围、RTA 绑定关系、联合实验 T+1 报表请求/返回字段与指标口径（不阻塞 mock MVP）。
4. **作品集页面**：问题、产品判断、AI 价值、方案权衡、验证结果五段式整理。
