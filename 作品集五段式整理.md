# 项目三 · 作品集五段式整理

> 用途：作品集展示页的核心内容 + 项目讲解稿骨架。
> 五段结构：① 问题 → ② 产品判断 → ③ AI 价值 → ④ 方案权衡 → ⑤ 验证结果。
> 配套：《项目三面试问答库》《项目三面试速记卡》《场景矩阵图》《PRD v0.2.1》。

---

## 第一段 · 问题

### 业务背景

广告主侧负责 RTA 投放和策略运营的优化师，在遇到"有预算但跑不出去"这类异常时，需要在 **RTAID 配置 → 账户绑定 → 策略工厂 → RTA 实验 → QPS → 报表 → 执行日志 → 归因回传** 8 个页面之间人工排查。

### 核心痛点

- **排查链路太长**：异常可能卡在 8 个环节的任何一个，且各环节数据分散在不同页面。
- **判断依赖经验**：即使看到指标异常，也很难快速判断"异常发生在哪一层、哪些证据支持、优先检查什么、下一步调什么"。
- **验证成本高**：调整策略后无法低风险验证判断是否正确。

### 一句话定义问题

> **投放异常排查不是"指标算不出来"，而是"判断路径太长"——需要把分散在多页面的多源信号，合成一条可解释的判断链。**

---

## 第二段 · 产品判断

### 产品定位

构建嵌入投放平台的 **RTA 投放诊断 Agent**：自动读取 RTAID、策略、实验、QPS、报表、执行日志等多源信号，识别异常场景、定位根因，生成带观察指标/成功标准/回滚条件的实验方案，由优化师人工确认后执行。

**它不是**：聊天式 Copilot、全自动执行 Agent。
**它是**：受控 Agentic Workflow（Human-in-the-loop）。

### 关键产品决策

| 决策 | 判断 | 依据 |
| --- | --- | --- |
| MVP 场景选"跑不出去" | 贯穿 RTA 全链路、资料依赖度最高、诊断链路清晰 | 与 RTAID/QPS/策略/实验/执行日志最相关 |
| 受控 Agent（不自动执行）| 高风险场景下可解释、可审核、可回滚比自动化更重要 | 真实业务对动态配置谨慎（延迟+维保成本）|
| 分层可解释诊断 | 同一份事实三层解读（管理摘要/运营解释/技术证据）| 服务管理者、运营、技术三类角色 |
| 嵌入式诊断抽屉 | 长在真实 RTAID 配置页内，不另起后台 | 降低用户迁移成本、贴合真实工作流 |
| 10 状态机闭环 | 诊断是完整业务闭环（发起→确认→执行→复盘）| "待人工确认"是 human-in-the-loop 的落点 |

### 产品形态

**投放管理列表页 + 右侧诊断抽屉（720px）**，入口为列表行级「发起诊断」按钮。

---

## 第三段 · AI 价值

### AI 到底做了什么（替代测试论证）

把 Agent 输出逐条过筛子"**能不能用确定性 if-else 生成**"：

| 输出类型 | 能否 if-else 写尽 | 归属 |
| --- | --- | --- |
| 判定（D1-D14 的 normal/abnormal）| ✅ 能（有限状态 + 阈值）| 规则引擎 |
| **归因**（多布尔值合成因果链）| ❌ 不能（因果方向是语义关系）| **AI** |
| **解释**（分角色自然语言）| ❌ 不能（开放文本空间）| **AI** |
| **规划**（生成带约束实验方案）| ❌ 不能（开放方案空间）| **AI** |

> **规则引擎回答"哪个环节出了问题"，AI 回答"为什么出问题、影响多大、下一步怎么验证"。判定是有限状态的可枚举逻辑，归因和规划是开放空间的因果推理与生成。**

### 三层架构

```
第 1 层 规则引擎 D1-D14   全链路体检（绑定/QPS/策略/实验/分桶/请求/命中/参竞/执行/出价/回传/预算/CPA）
第 2 层 场景识别 S0-S7    卡点定位（短路）+ 现象标注（预算未达标/CPA 过高/可安全放量）
第 3 层 AI 层            归因 / 解释 / 规划（接口对齐契约，Mock 模板占位，可换真实 LLM）
```

### AI 价值一句话

> **6 个典型问题不是 6 个引擎，而是"卡点 × 现象"的组合——规则引擎是全链路体检，场景是体检报告的不同解读方式，AI 负责把体检结果讲成人能听懂、能决策的话。**

---

## 第四段 · 方案权衡

### 关键取舍与理由

| 取舍 | 选择 | 权衡分析 |
| --- | --- | --- |
| 规则引擎 vs 纯 AI | 三层分工（规则判定 + AI 归因/解释/规划）| 硬判定确定性、可审计、不胡说；AI 做开放生成；两者解耦可独立替换 |
| 自动执行 vs 人工审批 | human-in-the-loop | 资金风险场景，AI 只生成草稿，人工确认后执行；真实业务动态配置有延迟+维保成本 |
| 单场景 vs 多场景 | 一个引擎 + 场景识别层 | 加场景不改引擎，只加"场景识别分支 + 叙事模板"，扩展成本可控 |
| 单文件离线 vs 工程化 | 零外网 + 普通 script（双击即开）| 作品集现场演示的可靠性优先（v1-v5 踩过 ESM/CDN 白屏的坑）|
| 全 Mock vs 接真实 | Mock + 真实字段口径 | 字段/阈值来自真实 PRD/截图，客户数据脱敏，媒体侧字段标【待核验】|

### 明确不做什么（边界）

- 不自动修改真实预算/出价/策略
- 不对接真实媒体 API（MVP 阶段）
- 不建设完整归因系统（归因仅作"效果可信度检查"）
- 不做实时消耗率监控（按自然日诊断）

---

## 第五段 · 验证结果

### 产品验证

| 维度 | 结果 |
| --- | --- |
| Golden Case 判定 | ✅ 主因 D9（门槛 40%→80% → 参竞率 60%→10%）、场景 S3、建议 80%→60%，10/10 通过 |
| 场景覆盖 | ✅ 8 场景 S0-S7 + 3 现象标签，6 类典型问题全部可诊断且深度一致 |
| 10 状态机 | ✅ 全状态可达（含数据不足、失败、重新诊断）|
| AI 层评测（L1-L4）| ✅ L1 判定一致 → L2 归因（主因/因果链/次因）→ L3/L4 解释与方案（三要素齐全）|
| 可靠性 | ✅ 数据不足不编造、Agent 边界声明、全局错误捕获 |

### Demo 演示指标

- 10 条 mock RTAID，覆盖全部 8 个场景 + 3 个现象标签
- 双击 file:// 即开，零外网依赖
- 6 类典型问题演示路径：跑不出去（Golden）→ CPA 过高 → 可安全放量 → 命中率 → 出价 → 回传

### 验证话术

> "我用 Golden Case 标准答案做了可执行的评测：判定 100% 一致、主因和因果链一票否决、解释和方案打分。Demo 内置 10 条覆盖全部场景的 mock 数据，任何一条都能演示完整诊断流程——这是'AI 价值可验证'的落地，不是自说自话。"

---

## 项目一句话（作品集标题用）

> **我设计了一个面向 RTA 投放优化的受控 Agentic Workflow：规则引擎做全链路判定，场景识别定位异常类别，AI 负责归因、解释和实验方案生成，并用 Golden Case 评测证明 AI 价值——在提升诊断效率的同时，通过人工审批保留了广告投放的风控边界。**

---

## 附：作品集页面素材清单

| 素材 | 来源 |
| --- | --- |
| 项目一句话 + 五段式 | 本文档 |
| 场景矩阵图（mermaid/表格）| `场景矩阵图.md` |
| Demo 演示视频/截图 | `demo/v6/index.html`（双击打开截图）|
| 三层架构图 | `PRD v0.2.1` §4.4 / `场景扩展方案` §1 |
| 面试问答 | `项目三面试问答库.md` + `项目三面试速记卡.md` |
| 数据口径说明 | `Mock数据口径说明.md` |
