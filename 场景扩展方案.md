# 场景扩展方案 v0.2（诊断引擎的多场景复用）

> 回答一个问题：**"这个 Agent 只能诊断一种情况吗？"** 答案是否——规则引擎是全链路体检，场景只是"体检报告的不同解读方式"。
> 本文档把"从单场景（有预算跑不出去）扩展到 6 个典型问题"落成可执行的判定规则表，供下一版 Demo 或 PRD 使用。
> 关联：《MVP 诊断规则表 v0.1》《AI 层设计与评测方案 v0.2》《RTA 对象模型与字段清单 v0.1》。

---

## 1. 核心思路

**规则引擎与场景解耦**：

```
┌ 第 3 层 · 场景叙事 ──────────────┐  ← 每个场景单独写（oneLiner/建议模板/入口文案）
├ 第 2 层 · 场景识别 detectScenario ┤  ← 按规则命中组合路由（本方案核心）
├ 第 1 层 · 规则引擎 D1-D14 ────────┤  ← 全链路检查，全场景复用，基本不改
└───────────────────────────────────┘
```

- 第 1 层（D1-D14）覆盖 RTA 全链路：绑定 → QPS → 策略 → 实验 → 分桶 → 请求 → 命中 → 参竞 → 执行 → 出价 → 回传 → 预算 → CPA。**扩展场景不换引擎**。
- 第 2 层（场景识别）是新增：根据"哪些规则命中、以什么组合命中"，判断当前属于哪个场景。
- 第 3 层（场景叙事）是每个场景补一套文案。

---

## 2. 场景识别模型：主判定（卡点定位）+ 辅标注（业务现象）

### 2.1 主判定：链路卡点定位（短路，按诊断链路顺序）

诊断链路从上游到下游是：请求量 → 命中率 → 参竞率 → 执行质量 → 出价 → 回传。**第一个卡点就是根因场景**：

| 场景 ID | 卡点 | 主判据（abnormal 信号） | 前提（上游须 normal） | 对应典型问题 |
|---|---|---|---|---|
| S0 | 配置故障 | D1/D2/D3/D4/D5 任一 abnormal | —（最高优先级短路） | 前置（非 6 问之一） |
| S1 | 入口流量 | D7 abnormal（请求量环比降 >50%） | — | ① 跑不出去（子类型 A） |
| S2 | 命中率 | D8 abnormal（命中率 <50%） | D7 normal | ③ 请求正常但命中率下降 |
| S3 | 参竞/放行 | D9 abnormal（参竞率差 >20pp） | D7/D8 normal | ① 核心 + ④ 子类型 |
| S4 | 出价/竞价 | D11 abnormal（出价越界） | D7/D8/D9 normal | ④ 命中正常但竞价失败 |
| S5 | 执行质量 | D10 abnormal（超时/失败/P99） | 上游 normal | —（服务异常，非 6 问） |
| S6 | 回传/归因 | D12 abnormal（回传成功率 <95%） | 上游 normal | ⑤ 前链路正常但 CVR 下降 |
| S7 | 无链路异常 | 全 normal | — | 进入现象标注层 |

> 说明：S0/S5 是配置故障与服务异常，不属于《案例采集草稿》的 6 个典型问题，但它们是诊断链路的前置短路项，必须保留（否则"跑不出去"会漏判根因）。

### 2.2 辅标注：业务现象（叠加在主场景上，或 S7 独用）

现象标注回答"业务上这是什么问题"，用于叙事和入口：

| 现象标签 | 判定 | 说明 |
|---|---|---|
| 预算未达标 | 达成率 < 60% | 现象：钱没花出去 |
| CPA 过高 | actualCpa > targetCpa | 现象：钱花出去了但贵（需新增 CPA 字段，见 §5） |
| 可安全放量 | 达成率高 且 CPA 达标 | 现象：跑得好，可加预算 |

### 2.3 场景 = 卡点 × 现象（最终映射到 6 个典型问题）

| 典型问题 | 场景组合 |
|---|---|
| ① 有预算但跑不出去 | S1/S2/S3/S4/S6（任一卡点）+「预算未达标」标签 |
| ② 能消耗但 CPA 过高 | 「CPA 过高」标签（常叠加 S4 出价 / S6 回传） |
| ③ 请求正常但命中率下降 | S2（+「预算未达标」标签，若消耗也降） |
| ④ 命中正常但竞价失败 | S3/S4 |
| ⑤ 前链路正常但 CVR 下降 | S6（+「CPA 过高」标签） |
| ⑥ 成本稳定后安全放量 | S7 +「可安全放量」标签 |

> 关键洞察：6 个典型问题**不是 6 个独立引擎**，而是"卡点定位"和"现象标注"两个维度的组合。一个诊断请求可以同时命中"卡点 = S3"和"现象 = 预算未达标"，即"① 跑不出去，根因在参竞率"——这正是当前 Golden Case 的情况。

---

## 3. detectScenario 判定树（可执行逻辑）

```
输入：signals[]（规则引擎 abnormal 信号）+ metrics（原始指标）
输出：{ sceneId, sceneName, phenomenonTags[], confidence }

短路判定（命中即停）：
1. D1-D5 任一 abnormal            → S0 配置故障
2. D7 abnormal                    → S1 入口流量
3. D8 abnormal（且 D7 未命中）     → S2 命中率
4. D9 abnormal（且 D7/D8 未命中）  → S3 参竞/放行
5. D11 abnormal（且上游未命中）    → S4 出价/竞价
6. D10 abnormal（且上游未命中）    → S5 执行质量
7. D12 abnormal（且上游未命中）    → S6 回传/归因
8. 全部未命中                      → S7 无链路异常

现象标注（叠加，不短路）：
- 达成率 < 60%                    → 追加「预算未达标」
- actualCpa > targetCpa          → 追加「CPA 过高」
- 达成率 > 90% 且 CPA 达标        → 追加「可安全放量」

置信度：取主判据信号的 confidence；S7 取现象标注的确定性（如达成率偏离阈值的程度）。
```

---

## 4. 各场景的叙事要点（第 3 层）

| 场景 | oneLiner 方向 | 建议方向 |
|---|---|---|
| S0 配置故障 | "RTAID 未上线/账户未绑定/…，链路未生效" | 先修配置，不进实验建议 |
| S1 入口流量 | "请求量环比下降，问题在媒体侧流量/创意/计划" | 不调 RTA 策略，排查媒体侧 |
| S2 命中率 | "命中率下降，人群包/模型阈值/字段匹配异常" | 回滚规则 / 扩人群 / 放宽字段 |
| S3 参竞/放行 | "策略门槛过严 → 参竞率降 → 消耗不足"（当前 Golden Case） | 门槛回调小流量试跑 |
| S4 出价/竞价 | "出价竞争力不足 → 竞价失败" | 提价实验 + 止损上限 |
| S5 执行质量 | "超时/失败率异常 → 服务/容量问题" | 优先排障，不调策略 |
| S6 回传/归因 | "回传异常 → 效果判断可能失真" | 提示"别调 RTA"，先修回传 |
| S7 无异常 | 依现象标签：未达标=深查；CPA 高=收人群；可放量=分层加预算 | 依现象标签 |

---

## 5. 第 1 层需要补的小改（唯一动引擎的地方）

当前 v6 规则引擎已覆盖 S0-S6 的判定，S7 负责无链路异常后的现象标注：

1. **CPA 判定字段已补齐**：v6 的 `budget` 包含 `conversionCount`、`actualCpa`、`targetCpa`，D14 负责 CPA 达标判定，结果只参与现象标签，不进入 S0-S7 短路链。

2. **达成率双向阈值**：D13 现为单向（<60% 判异常）。放量场景需反向：达成率 >90% 且 CPA 达标 → 可放量。这不必改 D13，可作为现象标注层的独立判断（不产生 abnormal signal，只打标签）。

> 结论：扩展 6 场景，规则引擎本身**几乎不动**，只需补 CPA 字段 + 现象标注逻辑。这与"加场景不换引擎"的判断一致。

---

## 6. 与现有代码的对接点

现有 `demo/V1.0/index.html` 里：
- `runRules(record)` → `signals[]` 已产出 D1-D14 判定 ✅
- `detectScenario(signals, record)` → `S0-S7 + phenomenonTags` ✅
- `runAiLayer(record, signals, scenario)` → 按场景生成归因、解释和建议 ✅
- `buildAssistantTemplateAnswer()` → 当前 RTA 范围内的 9 类受限问答 ✅

扩展时只需：
1. 已在规则输出和 AI 层之间接入 `detectScenario(signals, record)`，产出 `{ sceneId, phenomenonTags, confidence }`。
2. `runAiLayer` 已按 `scenario.sceneId` 分派场景模板，并保留安全兜底。
3. v6 列表页已展示场景/现象信息，诊断抽屉和智能参谋共用 `DiagnosisReport`。

---

## 7. 扩展路径建议

**当前 v0.6**：S0-S7、三类现象标签和各场景模板已落地，使用 Mock 数据运行。
**下一阶段**：将场景输入替换为可查询 Mock 数据，补充证据引用、Schema 校验和评测回归。
**后续阶段**：接入受控只读媒体工具，不改变场景识别和人工确认边界。

---

## 8. 当前结论

场景扩展不需要重写规则引擎。v6 通过“规则判定 -> 场景识别 -> AI/模板输出”复用同一套诊断事实；新增场景只需补充判定组合、现象标签、输出模板和对应评测样本。
