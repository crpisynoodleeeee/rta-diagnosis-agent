# 项目三资料盘点与可用性分析

## 1. 资料来源

本次盘点基于以下本地资料：

| 资料 | 路径 | 初步判断 |
| --- | --- | --- |
| RTA 截图 | `D:\Desktop\R (2)\R\PICTURE` | 真实系统页面截图，共 22 张 |
| RTA 华为接入及功能优化 PRD | `D:\Desktop\R (2)\R\R2.txt` | 项目三主线资料 |
| RTA 多媒体接入文档 | `D:\Desktop\R (2)\R\R_connect.txt` | 项目三主线资料 |
| RTA 基础能力文档 | `D:\Desktop\R (2)\R\R1.txt` | 项目三背景与模块补充 |
| RTA 同版补充文档 | `D:\Desktop\R (2)\R\R3.txt` | 与 R2 高度相似，作为校对资料 |
| 回传 API 汇总 | `D:\Desktop\R (2)\R\API_sum.txt` | 归因回传辅助资料 |
| 归因与回传调整文档 | `D:\Desktop\R (2)\R\REASON_1.txt` | 归因回传辅助资料 |
| 归因与回传口径文档 | `D:\Desktop\R (2)\R\reason_2.txt` | 诊断链路辅助资料 |
| Apple Ads 归因文档 | `D:\Desktop\R (2)\R\R_apple.txt` | 特定媒体归因资料，暂不作为主线 |

## 2. 总体结论

这些资料对项目三有直接帮助。项目三可以从“概念型 RTA Copilot”升级为“基于真实投放平台页面和规则的 RTA 策略诊断与实验 Copilot”。

建议项目三继续保持以下主线：

> 面向广告主侧 RTA 运营人员，在投放平台内提供策略诊断、异常定位、实验建议和复盘辅助的 Copilot。

其中：

- RTA 文档和截图作为主线输入。
- 归因与回传文档作为辅助诊断链路。
- Apple Ads 等特定媒体资料暂不扩展为项目核心，只在需要说明多媒体差异时引用。

## 3. 主线资料：RTA 部分

### 3.1 R2.txt 的价值

`R2.txt` 是当前最重要的项目三输入，文档名称为“RTA 华为接入及现有功能优化 PRD”，涉及页面包括：

- 策略工厂
- RTAID 配置
- RTAID 系统配置
- RTA 实验
- RTA 数据中心

可直接转化为项目三的以下内容：

| 可用内容 | 对项目三的作用 |
| --- | --- |
| RTA 技术原理 | 用于作品集解释业务背景 |
| 华为 RTA 接入流程 | 用于补全“媒体请求平台 RTA 接口”的链路 |
| 策略工厂 | 用于定义 Copilot 的策略配置诊断入口 |
| RTAID 配置 | 用于定义 RTAID、BidURL、SecretKey、媒体账户、QPS 等关键对象 |
| RTA 实验 | 用于定义 Copilot 生成实验方案和实验草稿的目标页面 |
| AB 实验分桶 | 用于定义实验设计、分桶异常、对照组兜底等诊断规则 |
| QPS 使用情况 | 用于“跑不出去/请求异常/容量不足”类问题诊断 |
| 出价配置 | 用于“CPA 过高/放量受限”类问题诊断 |
| RTA 数据中心 | 用于定义 Copilot 读取指标和展示诊断结果的入口 |

### 3.2 R_connect.txt 的价值

`R_connect.txt` 偏多媒体 RTA 接入和媒体差异处理，涉及小米、VIVO、OPPO、荣耀、华为等媒体。

它对项目三的价值是：

- 说明不同媒体的 RTAID 映射不完全一致。
- 支撑“媒体适配层”的产品判断。
- 提供出价能力矩阵和字段映射依据。
- 帮助 Copilot 在输出建议时区分媒体能力边界。

项目三不需要完整实现所有媒体差异，但可以在 PRD 中保留：

- 媒体字段差异
- RTAID 映射差异
- 出价能力差异
- 分桶能力差异
- 不支持能力时的提示和降级建议

### 3.3 R1.txt 的价值

`R1.txt` 更像 RTA 基础能力文档，包含：

- RTA 决策流程
- 策略工厂
- RTA 实验
- RTA 数据中心
- 执行状态枚举
- 执行失败原因
- RTA 监控指标

它适合作为项目三诊断规则的基础输入，尤其用于抽取：

- RTA 执行状态
- 执行失败原因
- 监控指标
- 报表筛选条件
- 数据中心指标口径

## 4. 辅助资料：归因与回传部分

归因和回传资料对项目三有帮助，但不建议成为项目主线。

项目三的核心仍是 RTA 策略诊断与实验优化。归因和回传应作为 Copilot 判断“效果异常是否可信”的辅助链路。

### 4.1 API_sum.txt 的价值

`API_sum.txt` 汇总了多媒体转化回传 API，核心价值包括：

- H5、小程序、APP 三类转化来源的回传识别方式。
- click_id、callback、callback_url 等关键参数差异。
- 多媒体回传字段、事件值、接口地址和必填规则。
- 媒体差异如何收敛为平台可配置字段。

可用于项目三中的“转化链路检查”：

- 转化事件是否具备回传所需标识。
- 媒体字段是否完整。
- 回传事件是否和媒体目标匹配。
- 回传失败是否影响 CPA/ROI 判断。

### 4.2 REASON_1.txt 的价值

`REASON_1.txt` 主要围绕归因配置、回传配置和回传日志。

可用于项目三中的“异常解释”和“诊断证据”：

- 回传账号配置不清晰可能导致回传错误。
- 回传条件配置不精细可能导致部分转化未回传。
- 分层回传能力不足可能导致回传比例与实际业务目标不一致。
- 回传日志字段和失败原因可以作为 Copilot 诊断依据。

### 4.3 reason_2.txt 的价值

`reason_2.txt` 对归因和回传口径更清晰，尤其有这些内容：

- 事件中心关联口径
- 归因配置调整
- 用户属性混合归因
- 归因/再归因日志
- 字段映射规则
- 回传识别字段
- 回传识别字段缺失处理

这份文档可以直接支持项目三的辅助诊断规则：

| 异常现象 | 可检查内容 |
| --- | --- |
| 转化下降 | 是否存在 click_id/callback 缺失 |
| CPA 异常升高 | 是否存在归因失败或回传失败 |
| ROI 异常 | 是否存在事件映射错误或回传金额缺失 |
| 实验效果不可信 | 是否存在 RTA 分桶字段、归因字段、回传字段不一致 |
| 数据断层 | 是否存在事件未关联投放链接或监测链接 |

## 5. 截图资料价值

`PICTURE` 文件夹中的截图具有很高价值，因为它们是实际平台页面证据。

已抽看的截图包括：

| 截图内容 | 对项目三的作用 |
| --- | --- |
| 策略工厂列表与策略详情 | 可提取策略字段、状态、数据源、字段配置、人群覆盖预估 |
| RTA 报表 | 可提取核心指标、筛选项、趋势图和资源使用情况 |
| 事件源配置 | 可辅助说明归因和回传依赖事件中心 |

截图可用于后续产出：

- 当前系统信息架构梳理
- Copilot 嵌入入口设计
- Demo 页面布局参考
- 作品集中的“现状系统分析”
- PRD 中的字段和状态说明

## 6. 对项目三范围的影响

### 6.1 应纳入 MVP 的内容

建议 MVP 聚焦以下范围：

1. RTA 数据中心异常诊断
2. 策略工厂配置检查
3. RTAID/QPS/媒体账户配置检查
4. RTA 实验配置检查
5. AI 生成实验优化建议
6. 归因与回传风险提示

### 6.2 暂不纳入 MVP 的内容

以下内容暂不纳入第一版 MVP：

- 完整建设 RTA 接口服务
- 自动修改真实投放策略
- 自动创建媒体广告计划
- 覆盖所有媒体的完整出价差异
- 完整归因系统重构
- 完整回传 API 管理平台
- Apple Ads 独立归因模块

### 6.3 可以作为增强项的内容

后续可作为增强项：

- 多媒体能力矩阵
- 媒体字段映射检查
- 回传日志异常解释
- 相似实验案例检索
- 实验复盘自动总结
- 策略变更影响分析

## 7. 推荐的 Copilot 场景

基于现有资料，项目三最适合优先做以下场景：

### 场景一：有预算但跑不出去

诊断链路：

```text
RTAID 状态
→ QPS 配置与剩余 QPS
→ 媒体账户绑定
→ 策略是否已发布
→ 实验是否生效
→ 分桶是否命中
→ 请求量、命中率、放行率、执行失败率
→ 出价配置是否过严
```

该场景和 R2/R_connect 的资料匹配度最高。

### 场景二：能消耗但 CPA 过高

诊断链路：

```text
放行率
→ 竞价/曝光/点击
→ 转化率
→ 出价方式
→ 策略人群覆盖
→ 转化回传是否完整
→ 归因口径是否稳定
```

该场景可以体现 RTA 与归因回传的联动。

### 场景三：实验效果不可信

诊断链路：

```text
AB 实验分桶方式
→ 平台分桶/客户分桶
→ 分桶字段是否完整
→ 对照组兜底是否过多
→ RTA 分桶 ID 是否能在归因日志中识别
→ 回传事件是否覆盖实验目标
```

该场景很适合展示产品设计深度，但实现复杂度略高。

## 8. 当前建议

第一版 PRD 建议选择“有预算但跑不出去”作为 MVP 主场景。

理由：

- 和 RTAID、QPS、策略状态、实验状态、执行日志最直接相关。
- 对 RTA 资料依赖最高，不容易偏到归因回传系统。
- 诊断路径清晰，适合做成可运行 Demo。
- 面试时容易解释 AI 价值：规则发现异常，AI 综合上下文解释原因并生成实验方案。

归因和回传资料建议在第一版中作为“效果可信度检查”出现，不独立扩张为主模块。

## 9. 后续转化任务

下一步建议按以下顺序推进：

1. 从 R2/R_connect/R1 中抽取 RTA 对象模型。
2. 从截图中整理真实页面字段和状态。
3. 形成项目三产品定义 v0.1。
4. 形成 Copilot 用户工作流。
5. 形成 MVP 场景的诊断规则表。
6. 形成 PRD v0.1。
7. 再决定是否进入 Demo 原型开发。

## 10. 待补问题

后续仍需你确认或补充：

1. 项目三是否优先选择“有预算但跑不出去”作为 MVP 主场景。
2. 真实截图是否允许用于作品集展示，还是只能作为内部设计参考。
3. RTA 报表中是否有真实非 0 数据截图。
4. 是否有一条真实问题案例，例如某个 RTAID 请求量、命中率、放行率或 CPA 异常。
5. 项目最终形态优先做 PRD、网页 Demo，还是 PRD + Demo 组合。

## 11. 已确认决策

截至当前，以上待补问题已有以下结论：

| 问题 | 当前结论 | 对项目三的影响 |
| --- | --- | --- |
| MVP 主场景 | 优先选择“有预算但跑不出去” | 第一版聚焦 RTAID、QPS、策略状态、实验状态、执行日志和数据中心诊断 |
| 截图使用方式 | 仅作为内部设计参考 | 作品集和后续前端展示需重新设计，不直接展示原系统截图 |
| 数据真实性 | 截图中既有真实数据，也有测试构造数据 | 后续统一使用 mock 数据，并明确标注为模拟数据 |
| 真实问题案例 | 当前没有真实客户场景案例 | 由项目基于现有业务链路构造脱敏/模拟案例 |
| 最终形态 | 先定项目框架，暂不决定 PRD 或 Demo | 当前阶段先完成产品定义、MVP 边界、诊断框架和 Agentic Workflow |

因此，项目三下一步不再继续补问基础方向，而是进入框架构建阶段。
